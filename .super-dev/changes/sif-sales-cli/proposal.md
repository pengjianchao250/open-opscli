# Proposal: Sif Sales CLI

## Status

Superseded by `.super-dev/changes/sif-traffic-compare-cli/`.

The user-facing entry is now platform-based:

```bash
opscli sif run 查销量 --asin B01NBNDC1T --site US
```

The old `opscli sales` router and `ops-sif-sales` Skill template have been removed. Sif platform guidance is maintained in `opscli/skills/templates/ops-sif/`.
