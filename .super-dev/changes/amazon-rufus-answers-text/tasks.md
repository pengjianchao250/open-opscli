# amazon-rufus answers text tasks

## 实施任务

- [x] 将 `amazon-rufus get` 成功输出改为默认 `answers[].text`。
- [x] 移除 `--answers-text` 参数。
- [x] 补充 CLI 单元测试覆盖默认纯文本输出。
- [x] 更新 `ops-amazon-rufus` README 与 SKILL 示例。
- [x] 运行 `tests/amazon_rufus/test_core.py` 验证回归。
