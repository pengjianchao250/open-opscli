# AI 取数能力底座一期联调验收记录

> 文档日期：2026-04-21  
> 范围：`auto-scheduler` + `opscli` + `dataset-fields` Skill  
> 结论：后端主链路已具备可用性，CLI/Skill 基础闭环已完成，本期进入联调收口阶段。

---

## 一、联调目标

本次联调关注一期最小闭环是否已经成立：

1. `auto-scheduler` 是否已提供带权限控制的取数与元数据接口
2. `opscli skills` 是否能完成安装、状态检查、升级与本地脚本调用
3. `dataset-fields` Skill 是否已具备可用的搜索、构造查询和本地更新能力
4. 各链路的配置、权限、版本、导出数据是否保持一致

---

## 二、联调范围

### 服务端接口

- `POST /api/v1/data-metrics/cli-query`
- `GET /api/v1/data-metrics/datasets/skill/export`
- `GET /api/v1/data-metrics/datasets/skill/export-datasets`
- `GET /api/v1/data-metrics/datasets/query-metadata`
- `GET /api/v1/data-metrics/datasets/skill/manifest`
- `POST /api/v1/data-metrics/datasets/skill/publish`

### CLI 与 Skill

- `opscli auth login`
- `opscli skills install dataset-fields`
- `opscli skills status`
- `opscli skills upgrade dataset-fields`
- `opscli query metadata`
- `opscli query build`
- `opscli query run`
- `dataset-fields/scripts/search.py`
- `dataset-fields/scripts/updater.py`
- `dataset-fields/scripts/query.py`

---

## 三、联调结论

### 3.1 已验证通过

- `cli-query` 后端链路已完成，包含数据集权限校验、受限字段权限校验、Python 查询代理与配置复用
- `skill/export`、`export-datasets`、`query-metadata` 已完成统一 payload 驱动
- `manifest/publish` 逻辑已完成
- `dataset-fields` 模板可安装、可升级、可本地搜索
- `opscli query` 已具备 metadata/build/run 三个入口
- `dataset-fields/scripts/query.py` 已统一转调 `opscli query`
- `skills status/upgrade` 已具备远端错误分类与排障信息

### 3.2 当前阶段仍未完全收口的点

- 已在本地 `ops.cm` 服务完成 `v0.1.0` 首次发布，`opscli skills` 成功样本已打通
- `opscli skills` 已补成功/失败两类真实环境联调记录，仍建议继续沉淀更多环境样本
- `cli-query` 自动化接口测试仍未补齐
- Skill 搜索仍是第一版加权排序，尚未引入 BM25 等更强索引能力

---

## 四、关键验证结果

## 4.1 服务端导出一致性验证

已新增验证脚本：

- `/Applications/MxSrvs/www/auto-scheduler/scripts/test-dataset-skill-exports.php`

验证结论：

- `datasets payload == query-metadata == datasets.csv`
- `fields payload == query-metadata == dataset_fields.csv`
- 当前真实用户验证样本中：
  - `datasets = 20`
  - `fields = 1755`

这说明：

- 数据集导出
- 字段导出
- 查询元数据

三份产物已经基于同一套权限过滤与组装逻辑，不存在额外漂移。

## 4.2 `query-metadata` 接口验证

已完成真实接口验证：

- `GET /api/v1/data-metrics/datasets/query-metadata`

验证结果：

- 接口返回 `200 OK`
- 用户权限过滤生效
- `datasets` 与 `fields` 引用关系一致
- 能够作为后续构造查询 JSON 的元数据来源

## 4.3 `cli-query` 配置复用修复验证

已完成以下配置收敛：

- `python_fetch_url`
- `python_api.connections.main.base_url`
- `python_api.connections.main.api_key`
- `from` 结构组装

对应实现：

- `/Applications/MxSrvs/www/auto-scheduler/vendor/aukey/data-metrics/src/Services/PythonQueryConfigResolver.php`

复用点：

- `CliQueryService`
- `CalculatedMetricApiController`
- `QueryBuilder::getPythonApiConfig()`

结论：

- 配置来源已统一
- `PYTHON_DATA_API_KEY` 已通过 `config/python_api.php` 正常参与解析
- 该修复已经过业务验证，结果正常

## 4.4 `opscli skills` 与模板脚本验证

已完成的本地能力：

- `opscli skills list`
- `opscli skills install dataset-fields`
- `opscli skills status`
- `opscli skills upgrade dataset-fields`
- `dataset-fields/scripts/search.py`
- `dataset-fields/scripts/updater.py`

当前模板脚本能力：

- `search.py` 支持关键词检索
- `search.py` 支持 `--dataset`
- `search.py` 支持 `-n/--limit`
- `updater.py` 支持 `--check`
- `updater.py` 支持 `--force`
- `updater.py` 支持 `--pretty`

## 4.5 自动化测试结果

当前 `tests/skills` 已通过：

- `22 passed`

覆盖范围包括：

- Skill 扫描
- 模板安装
- 版本比较
- 远端升级写入
- 搜索排序与数据集过滤
- 模板 updater 的 `--check` / `--force`
- 远端 404 与坏 JSON 异常分支
- `status` 的 `remote_summary`
- `skills install` 交互式多选
- `skills upgrade --force` 透传链路

## 4.6 `opscli skills` 真实环境命令级联调记录

本轮使用项目内 Typer 入口执行真实命令，原因是当前环境未安装全局 `opscli` 可执行文件。

认证状态：

- 当前账号：`chenbenli@aukeys.com`
- `ops` token：有效
- `polaris` token：无效/未获取

本地临时安装目录：

- `/Users/mask/python3/opscli/.tmp/phase1-integration-skills`

实际执行结果：

1. `skills install dataset-fields --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills`
- 返回成功
- 已写入 `SKILL.md`、`data/VERSION.json`、`dataset_fields.csv`、`datasets.csv`、`query_metadata.json`、模板脚本

2. 首轮 `skills status --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills --pretty`
- 返回成功
- 本地识别到已安装 `dataset-fields`
- 当前 `local_version = v0.0.0`
- 远端 `manifest` 返回 `[]`
- 因远端版本为空，`remote_version = null`
- 当前 `has_update = false`

3. 首轮 `skills upgrade dataset-fields --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills --force --pretty`
- 返回失败
- 失败原因：`远端 manifest 不存在`
- 该失败符合当前服务端发布态预期，不属于 CLI 本地逻辑错误

4. `POST /api/v1/data-metrics/datasets/skill/publish`
- 本地 `ops.cm` 服务已成功发布版本：`v0.1.0`
- 发布结果：
  - `dataset_count = 23`
  - `field_count = 2020`
  - `published_by = chenbenli@aukeys.com`

5. 发布后 `skills status --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills --pretty`
- 返回成功
- `local_version = v0.0.0`
- `remote_version = v0.1.0`
- `has_update = true`
- `remote_manifest` / `remote_summary` 均可正常返回版本信息

6. 发布后 `skills upgrade dataset-fields --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills --force --pretty`
- 返回成功
- 升级结果：
  - `from_version = v0.0.0`
  - `to_version = v0.1.0`
  - `field_count = 1755`
  - `tools = ["claude-code"]`

7. 升级后再次执行 `skills status --skills-dir /Users/mask/python3/opscli/.tmp/phase1-integration-skills --pretty`
- 返回成功
- `local_version = v0.1.0`
- `remote_version = v0.1.0`
- `has_update = false`

8. 本地数据文件校验
- `data/VERSION.json` 已更新为 `v0.1.0`
- `dataset_fields.csv`、`datasets.csv`、`query_metadata.json` 已更新为真实用户可见数据
- `upgrade --force` 的原子替换链路已通过真实样本验证

结论：

- `install` 本地落盘链路正常
- `status` 远端请求链路正常
- `publish` 本地服务发布链路正常
- `upgrade --force` 已完成从远端 manifest 到本地文件替换的真实成功验证
- 当前 CLI/Skill 侧主链路已打通

## 4.7 搜索到查询结果返回的端到端样本

已基于本地安装后的真实 Skill 数据跑通一条最小闭环：

1. 本地字段搜索
- 命令：
  `python /Users/mask/python3/opscli/.tmp/phase1-integration-skills/dataset-fields/scripts/search.py date_id --dataset ds_e93be345cfa1 -n 5`
- 命中字段：
  - `dataset_alias = ds_e93be345cfa1`
  - `field_name = date_id`
  - `field_type = dimension`
  - `is_dttm = 1`

2. 查询元数据确认
- 通过 `GET /api/v1/data-metrics/datasets/query-metadata`
- 确认：
  - `table_id = 1103`
  - `dataset_alias = ds_e93be345cfa1`

3. `cli-query` 最小请求
- 请求体核心结构：
  - `tableId = 1103`
  - `query.select = [{ "expr": "ds_e93be345cfa1.date_id", "alias": "date_id" }]`
  - `limit = 5`
  - `offset = 0`

4. 实际返回结果
- `POST /api/v1/data-metrics/cli-query` 返回 `200`
- 返回前 5 行数据：
  - `2020-02-01`
  - `2020-06-01`
  - `2022-01-01`
  - `2022-03-01`
  - `2022-03-01`
- 返回元信息：
  - `dataSource = doris_analytics`
  - `rowCount = 5`
  - `totalCount = 8200169`
  - `executionTimeMs = 1222`

结论：

- `search.py -> query-metadata -> cli-query -> 查询结果`
- 这条一期核心闭环已经在本地 `ops.cm` 环境完成真实成功验证

## 4.8 `opscli query build -> query run` 成功样本

已在本地 `ops.cm` 环境完成一条真实的 `build -> run` 成功样本：

1. `opscli query build`
- Skill 目录：
  - `/Users/mask/python3/opscli/.tmp/phase1-integration-skills`
- 数据集：
  - `ds_e93be345cfa1`
- 维度：
  - `country_name:f_dim001`
- 指标：
  - `price:sum:f_metric001`
- 筛选：
  - `country_name in ["美国"]`
  - `date_id between ["2022-01-01", "2022-03-31"]`
- 排序：
  - `f_metric001:desc`
- 生成 payload：
  - `/Users/mask/python3/opscli/.tmp/query-built-country.json`

2. `opscli query run`
- 基于上一步生成的 payload 文件直接执行
- 返回结果：
  - `success = true`
  - `rowCount = 1`
  - `executionTimeMs = 718`
  - `f_dim001 = 美国`
  - `f_metric001 = 75007659.6775`

结论：

- `opscli query build` 已能基于本地 metadata 组装合法 payload
- `opscli query run` 已能直接消费 `build` 结果并返回真实查询结果
- 查询执行入口已经从“手写 payload + 直连接口”收敛为“先 build，再 run”

## 4.9 `cli-query` 权限拒绝样本

已补一条真实 `403` 样本，用于验证数据集权限前置校验：

1. 先通过服务端权限查询确认
- 当前用户 `20` 可访问数据集数量：`20`
- 选取一个无权限数据集：
  - `tableId = 1256`
  - `dataset_alias = f_IxjHEPmDUbLq5JIE`

2. 调用 `POST /api/v1/data-metrics/cli-query`
- 使用最小请求：
  - `tableId = 1256`
  - `query.select = [{ "expr": "f_IxjHEPmDUbLq5JIE.date_id", "alias": "date_id" }]`

3. 实际返回
- HTTP 返回：`200`
- 业务返回：
  - `code = 403`
  - `msg = 无该数据集访问权限`

补充说明：

- 当前本地 `ops_metrics.dm_sql_metrics` 中 `is_restricted = 1` 的指标数量为 `0`
- 因此本轮无法补到“受限字段无权限”的真实 403 样本
- 但数据集权限拒绝样本已完成，可证明 `cli-query` 的前置权限校验链路生效

## 4.10 带筛选条件和分组的查询成功样本

已补一条更接近真实使用的聚合查询样本：

1. 查询目标
- 数据集：`ds_e93be345cfa1`
- `tableId = 1103`
- 维度字段：
  - `country_name`
- 指标字段：
  - `price`
  - 默认聚合：`SUM`

2. 请求结构
- `select`
  - `{ "expr": "ds_e93be345cfa1.country_name", "alias": "f_dim001" }`
  - `{ "expr": "ds_e93be345cfa1.price", "alias": "f_metric001", "aggregation": "SUM" }`
- `where`
  - `country_name in ["美国"]`
  - `date_id between ["2022-01-01", "2022-03-31"]`
- `groupBy`
  - `["f_dim001"]`
- `orderBy`
  - `[{ "expr": "f_metric001", "desc": true }]`

3. 实际返回
- `POST /api/v1/data-metrics/cli-query` 返回 `200`
- 返回数据：
  - `f_dim001 = 美国`
  - `f_metric001 = 75007659.6775`
- 返回元信息：
  - `rowCount = 1`
  - `totalCount = 1`
  - `executionTimeMs = 656`

补充说明：

- 首次尝试时使用了错误的 `orderBy` 结构：
  - `{ "field": "f_metric001", "direction": "DESC" }`
- Python 服务返回 `VALIDATION_ERROR`
- 按照查询服务文档修正为：
  - `{ "expr": "f_metric001", "desc": true }`
- 修正后查询成功

---

## 五、当前已完成的联调闭环

### 已完成闭环 1：后端元数据闭环

- `skill/export`
- `export-datasets`
- `query-metadata`
- 一致性校验脚本

### 已完成闭环 2：CLI/Skill 本地能力闭环

- 安装模板
- 检查远端状态
- 拉取远端数据
- 本地搜索
- 本地 updater 脚本执行

### 已完成闭环 3：服务端取数代理闭环

- `cli-query` 接口
- 配置复用
- 权限校验
- Python 查询代理

---

## 六、当前未完成的联调项

### 6.1 业务版本发布联调

当前 `manifest/publish` 逻辑和本地发布验证已完成，但仍建议补一轮更稳定的业务化发布验证：

- 形成正式约定的发布版本策略
- 验证更多用户样本下的 `skills status` / `skills upgrade --force`
- 保留正式发布留痕

### 6.2 真实环境命令级联调记录扩展

当前已补失败样本和成功样本各一轮，仍建议继续补充更多正式环境样本，至少覆盖：

```bash
opscli auth login
opscli skills install dataset-fields
opscli skills status --pretty
opscli skills upgrade dataset-fields --pretty
python .claude/skills/dataset-fields/scripts/search.py "GMV" -n 5
```

### 6.3 查询结果回流验收

已完成一轮最小端到端成功样本，后续仍建议补更多业务字段和权限场景：

1. 维度字段查询成功样本
2. 指标字段查询成功样本
3. 受限字段 403 样本
4. 带筛选条件与分组的查询样本

---

## 七、验收判断

### 已达到

- 后端基础接口可用
- CLI/Skill 基础能力可用
- 配置复用问题已修复
- 文档与计划已经同步

### 仍需继续推进

- 正式版本发布
- 真实环境命令级联调留痕
- 自动化接口测试补齐
- 搜索能力进一步增强

---

## 八、建议下一步

建议按以下顺序继续：

1. 先执行一次正式 `publish`
- 形成首个有效远端版本号

2. 再做真实环境命令级联调记录
- 重点记录 `skills status` / `skills upgrade`

3. 然后补 `cli-query` 自动化接口测试

4. 最后按优化项推进搜索排序和索引能力
