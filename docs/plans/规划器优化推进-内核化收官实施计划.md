# 规划器优化推进 · 内核化收官实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐内核 `opscli query flow` 执行段的三个延后项，金样对照通过后把 SKILL.md 主线入口从 Skill Python 脚本切换到内核命令，消灭"环境缺 Python"死穴。

**Architecture:** 三个延后项均为**从 skill `run_query.py` 向内核 `planner/entry.py` 的能力移植**——源实现已在生产验证，移植遵循"行为等价优先，不顺手改口径"。切换采用过渡策略：SKILL.md 主入口改 `opscli query flow`，旧脚本降级备选保留一个版本周期。

**Tech Stack:** Python 3.10 + pytest；金样对照复用 `tests/query/planner/golden` 既有基建。

**Spec:** `docs/plans/规划器优化推进-总体规划.md`

## Global Constraints

- 工作目录 /Users/mask/python3/opscli（release 直提）；铁律17/18/8 同前；tests 基线口径同快速止血计划
- **行为等价铁律**：移植任务（K1-K3）以 skill `run_query.py` 现行为为规范，差异必须在任务报告中逐条列出并说明理由；本计划不改进算法，只搬家
- 前置：快速止血计划 C1-C3 已完成（内核侧 dataset_guidance/query_plan/enum 已同步），否则切主线后行为漂移
- `planner/entry.py:26-35` 的两个延后披露常量（`_NOTE_ORDER_BY`/`_NOTE_RESULT_DIR`）是本计划的"完成标志"：对应能力落地后必须删除该披露

---

### Task K1: orderBy 本地兜底迁入内核

**Files:**
- Modify: `opscli/query/services/planner/entry.py`（`run_flow` 执行段）
- 源实现（只读参照）: `opscli/skills/templates/ops-dataset-query/scripts/run_query.py` 的 `_sort_value`（:393）、`_is_monotonic`（:406）、排序放大重查逻辑（`_run_opscli` 调用链 :444/:461 附近的放大倍数与硬上限常量 :35）
- Test: `tests/query/planner/test_entry.py`（追加）

**Interfaces:**
- Produces: `run_flow(order_by=[{"field": F, "desc": bool}])` 执行后对结果做单调性校验；不单调→本地重排；行数达 limit 且不单调→按源实现的放大倍数加量重查一次再本地重排截断；披露键记录兜底动作（键名沿用 skill 版 disclosures 的对应键，保持 Agent 侧文档兼容）
- 完成标志：删除 `_NOTE_ORDER_BY` 常量及其挂载点

- [ ] **Step 1: 通读源实现**，在报告里写出移植清单（函数、常量、披露键名、放大上限值——逐项照抄源值）
- [ ] **Step 2: 写失败测试**

```python
def test_flow_order_by_local_resort_when_server_ignores(monkeypatch):
    """服务端返回乱序时本地重排（QA 实测服务端 orderBy 有时不生效）。"""
    # mock 执行回调返回乱序行，断言 run_flow 返回行按 desc 单调，且披露含本地重排标记
def test_flow_order_by_amplified_requery_on_full_page(monkeypatch):
    """结果行数=limit 且乱序：加量重查一次再截断（TopN 拿错行的防线）。"""
    # mock 第一次返回 limit 行乱序、第二次返回放大后的行集，断言最终 TopN 正确且只重查一次
```

- [ ] **Step 3: 移植实现 → Step 4: 测试通过 + `tests/query/planner/` 全量回归 → Step 5: change-log + 提交** `✨ feat(query): 内核 flow 补齐 orderBy 本地兜底与加量重查`

---

### Task K2: result_dir 全量落盘 + 预览限幅迁入内核

**Files:**
- Modify: `opscli/query/services/planner/entry.py`
- 源实现（只读参照）: `run_query.py` 的全量落盘与 stdout 限幅（模块 docstring :10 声明的"全量结果落盘只回预览+披露"，落盘文件名/预览行数/8KB 限幅等源常量照抄）
- Test: `tests/query/planner/test_entry.py`（追加）

**Interfaces:**
- Produces: `run_flow(result_dir=Path)` 时全量结果写入该目录（文件名模式与 skill 版一致，如 `query_result_<ts>.json`），合同只含预览行 + `full_result_file` 披露；未传 result_dir 行为不变
- 完成标志：删除 `_NOTE_RESULT_DIR` 常量及其挂载点；`query flow --result-dir` 帮助文案去掉"当前忽略"

- [ ] **Step 1: 移植清单 → Step 2: 失败测试**（传 result_dir：断言文件落盘、内容为全量行、合同 preview 行数受限且含 full_result_file；tmp_path 隔离）→ **Step 3: 实现 → Step 4: 回归 → Step 5: change-log + 提交** `✨ feat(query): 内核 flow 补齐全量落盘与预览限幅`

---

### Task K3: evidence_contract 迁入内核

**Files:**
- Create: `opscli/query/services/planner/evidence_contract.py`（从 skill `scripts/evidence_contract.py` 移植，去 CLI 壳保留纯函数）
- Modify: `entry.py`（flow 输出内嵌证据合同，键名与 skill 版 `run_query.py` 输出一致）
- Test: `tests/query/planner/test_evidence_contract.py`（新建；断言样例从 skill 版行为取真值）

**Interfaces:**
- Produces: flow 合同含 `evidence_contract` 键，结构与 skill 主线完全一致（Agent 侧 SKILL.md/result-analysis.md 的消费文档零改动）

- [ ] **Step 1: 移植清单（skill 版输出键集快照）→ Step 2: 失败测试 → Step 3: 实现 → Step 4: 回归 → Step 5: change-log + 提交** `✨ feat(query): 内核 flow 内嵌证据合同`

---

### Task K4: 金样对照回归

**Files:**
- Create: `tests/query/planner/test_flow_parity.py`
- 基建（只读参照）: `tests/query/planner/golden/`、`fixtures/`、`gold/` 既有对照模式

**Interfaces:**
- Produces: 一组代表性请求（至少覆盖：单维度单指标、带时间口径、带排序 TopN、带组件筛选、澄清场景）下，内核 `run_plan/run_flow` 与 skill `query_plan.build_query_plan` 的合同关键字段一致：选表 `table_id`、`query_template` 的 dimensions/metrics/filters 集合、`status`、澄清消息键集。执行段字段（披露/证据合同）键集一致
- 差异白名单机制：确因架构不同的字段（如 metadata 来源标注）列入显式白名单并注释理由，白名单外差异即失败

- [ ] **Step 1: 写对照测试**（skill 模块经 sys.path 注入 import，与 `tests/skills/` 既有做法一致；元数据用 `tests/query/planner/fixtures` 与 skill 侧同一份数据源，保证输入等价）
- [ ] **Step 2: 跑通并修复暴露的等价性缺口**——缺口修复原则：内核向 skill 版对齐（skill 版是生产行为）；确属 skill 版 bug 的例外须报告列明并按快速止血计划同修双份
- [ ] **Step 3: 回归 + change-log + 提交** `✅ test(query): 内核 flow 与 skill 主线金样对照`

---

### Task K5: 主线切换与文档收口

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/SKILL.md`（主线入口、降级备选说明、版本号）
- Modify: `opscli/skills/templates/ops-dataset-query/references/cli.md`（flow 命令为主入口的参数文档）
- Modify: `opscli/skills/templates/ops-dataset-query/data/VERSION.json`（1.3.21 → 1.3.22）
- Test: `tests/skills/test_dataset_query_flow.py`（文案锁定断言同步更新）
- 前置：K1-K4 全部完成且金样对照通过

**Interfaces:**
- Produces: SKILL.md 主线入口 = `opscli query flow "<用户请求>"`；旧 `python3 scripts/query_flow.py` 降级为"备选执行通道（环境异常时）"，保留一个版本周期后另行下线；降级层级表 L 系口径同步（规划器不可用的判据从"脚本报错"扩展为"命令报错"）

- [ ] **Step 1: SKILL.md 改写主线段落**（保持既有铁律文案锁定测试可过：先读 `test_dataset_query_flow.py` 的断言清单，锁定文案有语义冲突的逐条同步）
- [ ] **Step 2: cli.md 补 flow 主入口文档**（参数逐个列全：request/--query-file/--field/--limit/--order-by/--offset/--result-dir/--pretty；示例含 TopN 与落盘）
- [ ] **Step 3: 版本号同步 1.3.22 → Step 4: 回归** `python3 -m pytest tests/skills/test_dataset_query_flow.py tests/skills/test_local_fallback.py tests/query/planner/ -v`
- [ ] **Step 5: E2E 冒烟**：`opscli query flow "近7天各渠道订单量前3"` 真实出数（QA 环境，OPSCLI_OPS_URL 按会话现值）；对照 skill 版同请求结果行一致
- [ ] **Step 6: change-log + 提交** `✨ feat(skills): 主线入口切换为内核 opscli query flow，版本 1.3.22`
