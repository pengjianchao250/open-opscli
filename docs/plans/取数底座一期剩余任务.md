# AI 取数能力底座一期剩余任务清单

> 文档日期：2026-04-21  
> 基于当前实际进度整理：后端主链路已完成，`opscli skills/query` 与 `dataset-fields` Skill 已基本完成，当前主要剩收口与增强项。

---

## 一、当前完成情况

### 已完成模块

- 模块 A：`auto-scheduler cli-query`
- 模块 B：`skill/export`
- 模块 C：`query-metadata`
- 模块 D：`manifest/publish`

### 部分完成模块

- 模块 E：`opscli skills/query`
- 模块 F：`dataset-fields` Skill 模板

### 待完成模块

- 模块 G：联调、验收、文档收口

---

## 二、剩余任务总览

### P0：优先收口的一期必做项

1. 完成 `opscli skills` 的真实联调闭环
- 已验证 `opscli auth login` 后，`skills status`、`skills upgrade --force` 在本地 `ops.cm` 环境可成功
- 确认 `OPS_URL` / `ops_url` 指向的环境已经部署对应接口
- 已补真实 token + 真实环境的命令级验证记录
- 剩余重点是补更多正式环境样本

2. 完成 `dataset-fields` Skill 的可用搜索闭环
- 已完成“关键词检索 + 数据集过滤 + topN 返回”的第一版能力
- 剩余重点为索引能力增强和更智能的排序策略

3. 完成一期联调验收
- 已沉淀联调验收记录文档初稿
- 已完成本地 `ops.cm` 环境下 `search.py -> opscli query build/run` 成功样本
- 已完成“无数据集权限”的真实 `403` 样本
- 剩余重点是补更多业务字段、受限字段和分组筛选场景

### P1：建议本期内补齐项

1. 补 `skills/query` 的真实集成测试
- 覆盖 `status`
- 覆盖 `upgrade`
- 覆盖 `--force`
- 覆盖 `query build`
- 覆盖 `query run`
- 远端 `404` / 坏 JSON 的异常分支已补
- 仍建议补真实远端环境下的联调测试

2. 补 `cli-query` 的自动化接口测试
- controller 层基础测试已补
- service 层基础测试已补
- 剩余重点是真实数据场景测试

3. 完善模板内 `scripts/updater.py`
- 已完成独立入口版本
- 当前剩余的是与真实环境联调验证，而不是脚本能力缺失

4. 正式发布一个非 `v0.0.0` 的 manifest 版本
- 已在本地 `ops.cm` 服务发布 `v0.1.0`
- CLI 已可正确感知并执行升级
- 已补发布检查清单，可继续沉淀正式留痕

### P2：可后置优化项

1. 搜索能力增强
- BM25 或更合理的打分排序
- `keywords` / `verbose_name` / `description` 分字段权重
- `query_metadata.json` 的本地辅助检索

2. `skills` 命令体验优化
- 批量升级多个 Skill
- 更清晰的 pretty 输出
- 更细的错误码与失败原因分类

3. 文档补齐
- 更新 `README.md`
- 更新 `/Users/mask/python3/opscli/docs/opscli-guide.md`
- 增加 `skills` 命令示例

---

## 三、按模块拆分的未完成项

## 模块 E：opscli skills / query

### 已完成

- `skills list`
- `skills install`
- `skills status`
- `skills upgrade`
- `query metadata`
- `query build`
- `query run`
- `SkillDetector`
- `SkillsManager`
- `SkillsUpdater`
- 默认 JSON 结构已对齐需求文档
- 交互式多选安装体验
- 基础测试
- CLI JSON 契约测试
- `upgrade --force` 自动化验证

### 未完成

- 更完整的命令文档
- 更多环境样本下的联调留痕
- `query build -> run` 的更多真实业务样本

## 模块 F：dataset-fields Skill

### 已完成

- 模板目录
- `SKILL.md`
- `data/VERSION.json`
- `dataset_fields.csv`
- `datasets.csv`
- `query_metadata.json`
- 基础 `search.py`
- `query.py`
- Skill 查询统一转调 `opscli query`

### 未完成

- `core.py` 的索引能力增强

## 模块 G：联调与验收

### 未完成

- 更多复杂场景下的全链路验收
- 标准验收脚本

---

## 四、推荐下一步执行顺序

1. 先把 `manifest` 正式发布一个有效版本
- 已完成，本地 `ops.cm` 已发布 `v0.1.0`

2. 接着补 `dataset-fields` 搜索能力
- 这一项基础能力已完成，下一步更偏索引增强

3. 然后跑完整联调
- `auth`
- `skills install`
- `skills upgrade`
- `search.py`
- `query-metadata`
- `cli-query`
- 其中 `skills install/status/upgrade` 在本地 `ops.cm` 已完成成功联调

4. 最后补文档和测试
- CLI 使用说明
- 联调记录
- 自动化测试缺口

---

## 五、建议的一期收尾标准

满足以下条件后，可以认为一期基本收口：

1. `auto-scheduler` 四类接口全部稳定可用
- `cli-query`
- `skill/export`
- `query-metadata`
- `manifest/publish`

2. `opscli skills` 可在真实环境完成安装、检查、升级

3. `dataset-fields` Skill 可完成本地搜索，并通过 `opscli query` 辅助查询构造与执行

4. 至少有一份完整联调记录和一份用户使用说明
