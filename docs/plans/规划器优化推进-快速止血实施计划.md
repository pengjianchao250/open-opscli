# 规划器优化推进 · 快速止血实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 堵住 8 月 QA 数据实证的四个高频出血点：元数据 dup 事故、旧版客户端混跑、未授权筛选注入、组件枚举超时。

**Architecture:** 服务端两项（导出 dup 防线、版本门槛配置化）与客户端三项（dup 自愈、注入前权限交集、枚举缓存）互不依赖、全部独立可发。客户端改动凡涉及 Skill 脚本与内核 planner 双份文件的必须同步修改（防止内核化切换后行为漂移）。

**Tech Stack:** Laravel/PHPUnit（服务端）、Python 3.10 + pytest（opscli）。

**Spec:** `docs/plans/规划器优化推进-总体规划.md`（决策默认值以总纲为准）

## Global Constraints

- opscli 仓库（/Users/mask/python3/opscli，分支 release 直提）：铁律17 中文注释说明"为什么"；铁律18 每任务提交前向 `docs/change-log-pending.md` 追加五段式变更记录；铁律8 测试无真实网络/Keychain；tests/skills 既有 8 失败与 25 collection error 为基线，不得新增
- 服务端仓库：auto-scheduler（release 直提）与 vendor/aukey/data-metrics 包（独立 git 仓库，release）；共享 QA 库测试纪律：禁 RefreshDatabase/truncate，tearDown 按 id 精确清理
- **双份同步铁律**：`opscli/skills/templates/ops-dataset-query/scripts/` 与 `opscli/query/services/planner/` 存在同名模块（dataset_guidance/query_plan 等），本计划改到的逻辑必须两份同改、各自有测试
- 总纲决策默认值不再讨论：min_version=0.0.153（仅 QA）、服务端 dup=去重+告警、客户端 dup=完全重复自愈/同名异义阻断、枚举缓存 TTL 24h

---

### Task S1: 服务端字段导出 dup 防线

**Files:**
- Modify: `vendor/aukey/data-metrics/src/Services/DatasetSkillService.php`（`buildExportPayloadForUser` 的 fields 出口，~77 行起）
- Test: `vendor/aukey/data-metrics/tests/`（跟随包内既有可运行测试模式；若包环境跑不通则按 A3 先例落主仓库 `tests/Unit/DataMetrics/`）

**Interfaces:**
- Produces: fields 导出保证 `(dataset_alias, field_name)` 唯一；发现重复时保留首条并 `Log::warning('DatasetSkillService duplicate field dropped', [...])` 记录 dataset_alias/field_name/重复次数

- [ ] **Step 1: 写失败测试**

构造含完全重复字段行的 fields 集合（同 dataset_alias + 同 field_name 两行），断言导出结果该键只出现一次，且不抛异常：

```php
public function test_export_fields_deduplicates_duplicate_rows(): void
{
    // 通过反射或提取的纯函数测 dedup 逻辑：输入含 (ds_x, sales) 两行，
    // 输出仅一行且保留首条的 summary_expression
    $rows = collect([
        ['dataset_alias' => 'ds_x', 'field_name' => 'sales', 'summary_expression' => 'SUM(a)'],
        ['dataset_alias' => 'ds_x', 'field_name' => 'sales', 'summary_expression' => 'SUM(b)'],
        ['dataset_alias' => 'ds_x', 'field_name' => 'qty',   'summary_expression' => ''],
    ]);
    $result = DatasetSkillService::dedupeFieldRows($rows);   // 新增的静态纯函数
    $this->assertCount(2, $result);
    $this->assertSame('SUM(a)', $result->firstWhere('field_name', 'sales')['summary_expression']);
}
```

- [ ] **Step 2: 运行确认失败**（方法不存在）

- [ ] **Step 3: 实现**

新增静态纯函数 `dedupeFieldRows(Collection $rows): Collection`：按 `dataset_alias.'|'.field_name` `unique()` 保留首条；去重前后数量不等时 `Log::warning` 带被丢弃键清单。在 `buildExportPayloadForUser` 的 fields 流水线出口调用（CSV 导出与 query-metadata 两条出口都要经过——确认两条出口共用该流水线，若不共用则各自接入）。中文注释写明背景：2026-08-11 dup 事故两天 69 条客户端 blocked。

- [ ] **Step 4: 测试通过 + 既有导出测试回归**

- [ ] **Step 5: 提交（包仓库）** `fix: 字段导出按 (dataset_alias, field_name) 去重防线，重复行告警丢弃`

---

### Task S2: 版本门槛配置化启用（QA）

**Files:**
- Modify/Create: `auto-scheduler` 主仓库 `config/opscli.php`（若不存在则创建：`version_check_enabled` / `min_version` / `update_guide_url` 三键均读 env）
- Test: `auto-scheduler/tests/Feature/OpscliVersionGateTest.php`
- 部署项（不改代码）：QA `.env` 增加 `OPSCLI_VERSION_CHECK_ENABLED=true`、`OPSCLI_MIN_VERSION=0.0.153`，写入本任务报告供部署执行

**Interfaces:**
- Consumes: 既有 `app/Http/Middleware/OpscliVersionMiddleware.php`（已实现 `config('opscli.version_check_enabled', false)` + `version_compare` 拒绝逻辑，勘察确认）
- Produces: 低于 min_version 的 opscli 请求收到中间件既有的拒绝响应（含 min_version 与升级指引）

- [ ] **Step 1: 确认现状**

读 `config/opscli.php` 是否存在及键名；读 middleware 拒绝分支的 HTTP 状态码与响应结构（测试断言要用真实值，不要猜）。确认 `routes/api.php` 的 cli（:112）与 mcp（:124）路由组均已挂 `opscli.version` 中间件。

- [ ] **Step 2: 写失败测试**

```php
public function test_outdated_client_rejected_when_gate_enabled(): void
{
    config(['opscli.version_check_enabled' => true, 'opscli.min_version' => '0.0.153']);
    $resp = $this->withHeaders(['X-Opscli-Version' => '0.0.124'])   // Header 名以 middleware 实际读取为准
        ->getJson('/api/v1/cli/...既有任一挂中间件的轻端点...');
    // 断言为 middleware 真实拒绝形态（Step 1 查明后填具体状态码/信封字段）
    $resp->assertStatus(/* 实际值 */);
}

public function test_gate_disabled_by_default(): void
{
    config(['opscli.version_check_enabled' => false]);
    // 同请求不被版本逻辑拒绝
}
```

- [ ] **Step 3: 实现 config 文件（若缺）+ 测试通过**

- [ ] **Step 4: 提交（主仓库）** `feat: opscli 版本门槛配置化（QA 启用 min_version=0.0.153 由部署配置）`，报告附 QA .env 两行配置

---

### Task C1: 客户端 dup_field 自愈（双份同步）

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/dataset_guidance.py`（`_validated_dataset_fields`，~270 行）
- Modify: `opscli/query/services/planner/dataset_guidance.py`（同名函数 ~270 行，两份当前逐行同构）
- Test: `tests/skills/test_dataset_guidance_dup_selfheal.py`（新建）+ `tests/query/planner/test_guidance.py`（追加用例）

**Interfaces:**
- Produces: `_validated_dataset_fields` 行为分级——**整行完全重复**（所有列相同）时静默去重并通过，返回值携带去重信息供上层写入披露；**同名不同定义**维持 `raise ValueError("duplicate_dataset_field")`
- 上层披露：plan 合同 `model_view` 增加 `metadata_degraded_zh`（如"元数据含 N 行完全重复字段已自动去重，建议执行 opscli skills upgrade 刷新"）——两份 query_plan/guidance 的接线点由 implementer 按各自现有披露键模式接入

- [ ] **Step 1: 写失败测试**

```python
def test_fully_duplicate_rows_are_self_healed():
    """整行完全重复（2026-08-11 事故形态）应静默去重通过，而不是 blocked。"""
    rows = [
        {"dataset_alias": "ds_x", "dataset_name": "表X", "field_name": "sales", "field_type": "metric", "summary_expression": "SUM(a)"},
        {"dataset_alias": "ds_x", "dataset_name": "表X", "field_name": "sales", "field_type": "metric", "summary_expression": "SUM(a)"},
        {"dataset_alias": "ds_x", "dataset_name": "表X", "field_name": "qty", "field_type": "metric", "summary_expression": ""},
    ]
    selected = _validated_dataset_fields(rows, {"dataset_alias": "ds_x", "dataset_name": "表X"})
    assert [r["field_name"] for r in selected] == ["sales", "qty"]


def test_same_name_different_definition_still_blocks():
    """同名不同定义去重会静默选错口径，必须维持阻断。"""
    rows = [
        {"dataset_alias": "ds_x", "dataset_name": "表X", "field_name": "sales", "field_type": "metric", "summary_expression": "SUM(a)"},
        {"dataset_alias": "ds_x", "dataset_name": "表X", "field_name": "sales", "field_type": "metric", "summary_expression": "SUM(b)"},
    ]
    with pytest.raises(ValueError, match="duplicate_dataset_field"):
        _validated_dataset_fields(rows, {"dataset_alias": "ds_x", "dataset_name": "表X"})
```

（两个测试文件各放一份，分别 import 各自模块；字段行 dict 的键集按各自模块实际消费的列补齐，跑失败时以真实 KeyError 为准补列。）

- [ ] **Step 2: 确认失败 → Step 3: 实现（两份文件相同改法）**

```python
    # 完全重复行先去重：2026-08-11 元数据事故（服务端下发整行重复）两天打出 69 条
    # blocked——事故形态可安全自愈；同名不同定义仍阻断，静默取其一会选错口径
    deduped = list({tuple(sorted(row.items())): row for row in selected}.values())
    names = [row["field_name"] for row in deduped]
    if len(names) != len(set(names)):
        raise ValueError("duplicate_dataset_field")
```

去重发生时经返回值/上下文把条数带给上层披露（接线方式跟随各自模块现有披露模式，保持两份行为一致）。

- [ ] **Step 4: 双侧测试通过 + 回归** `python3 -m pytest tests/skills/test_dataset_guidance_dup_selfheal.py tests/query/planner/ -v`

- [ ] **Step 5: change-log + 提交** `🐛 fix(query): 元数据整行重复字段自愈去重，同名异义仍阻断（双份同步）`

---

### Task C2: 未授权筛选注入修复（双份同步）

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/query_plan.py`（组件值注入 filters 的路径——从 QA 反馈形态 `query_flow 生成未授权的 country_name/brand_name 筛选` 反查：语义命中组件值 → 写入模板 filters 的函数）
- Modify: `opscli/query/services/planner/query_plan.py`（同名逻辑）
- Test: `tests/skills/test_unauthorized_filter_injection.py`（新建）+ 内核侧对应用例

**Interfaces:**
- Produces: 组件语义值注入 filters 前，必须先与该组件的**权限枚举结果**求交集：有交集→注入交集值；枚举成功但无交集→**不注入**，在 `clarification_messages_zh`（或该模块现行澄清键）披露"识别到 X=Y 但当前账号无该值权限，未注入筛选，请确认"；枚举失败/超时→不注入并披露未校验

- [ ] **Step 1: 定位注入路径**

在两份 query_plan.py 中 grep 组件值写入 filters 的函数（线索：`_platform_component_lookup`、`resolved_filter_values`、filters 模板组装处），并找到 QA 事故的复现输入（如查询原文含品牌词/国家词且该值不在账号枚举中）。将定位结论写入任务报告（函数名+行号）。

- [ ] **Step 2: 写失败测试**

构造：语义命中组件值 "德国"（country_name），mock 枚举返回 `["美国","加拿大"]`（无交集）：

```python
def test_unauthorized_component_value_not_injected():
    """语义命中但账号枚举无该值：不得注入 filters（否则执行器 precheck 硬拒，
    QA 实测形态 VCUR-007 precheck_failed: country_name unauthorized filter）。"""
    plan = build_query_plan("查德国站近7天销量", ..., enum_fn=fake_enum_us_ca_only)
    filters = plan["execution_ref"]["query_template"]["filters"]
    assert not any(f.get("field") == "country_name" for f in filters)
    assert any("德国" in m for m in plan["model_view"].get("clarification_messages_zh") or plan_disclosures(plan))
```

（`enum_fn`/mock 方式按 Step 1 定位到的真实注入函数签名适配；断言意图不变：未授权值不落 filters + 有面向用户的披露。）

- [ ] **Step 3: 实现（交集校验，两份同步）→ Step 4: 双侧测试 + 回归（重点跑 `tests/skills/test_component_filter_resolution.py` 66 用例防误伤）**

- [ ] **Step 5: change-log + 提交** `🐛 fix(query): 组件值注入 filters 前做权限枚举交集校验（双份同步）`

---

### Task C3: 组件枚举缓存 + 超时降级（双份同步）

**Files:**
- Create: `opscli/skills/templates/ops-dataset-query/scripts/enum_cache.py` 与 `opscli/query/services/planner/enum_cache.py`（同构小模块）
- Modify: 两份 `query_plan.py` 的三处枚举调用（`_auto_enum_platform_values` :2384、:2453、:2755 附近的 `timeout_seconds=7.0` 路径）与内核 `entry.py` 的 `enum_fn`
- Test: `tests/skills/test_enum_cache.py` + 内核侧用例

**Interfaces:**
- Produces: `enum_cache.get(table_id, field_name) -> list[str] | None`（TTL 24h 内命中）；`enum_cache.put(table_id, field_name, values)`；缓存文件 `CONFIG_DIR/enum_cache/{table_id}_{field_name}.json`（含 fetched_at）。枚举流程改为：先实时枚举，成功→put+返回；超时/失败→get，命中→返回缓存值并在披露标注"枚举值来自 N 小时前缓存"；缓存也无→维持现行失败路径
- Skill 侧无法 import opscli 包：Skill 版 enum_cache 用 `os.path.expanduser("~/.config/opscli/enum_cache")` 直接拼路径（与 CONFIG_DIR 同址），内核版 `from opscli.config import CONFIG_DIR`

- [ ] **Step 1: 写失败测试**（put→get 命中；过期不命中；损坏文件安全返回 None；超时降级路径返回缓存并带 stale 披露）
- [ ] **Step 2: 实现 enum_cache 模块（两份）**——文件读写原子（tempfile+rename）、任何 IO 异常静默返回 None（缓存永不阻断主流程，中文注释写明）
- [ ] **Step 3: 接线三处枚举调用与内核 enum_fn，超时分支降级读缓存**
- [ ] **Step 4: 测试 + 回归（tests/skills 基线对照；实测一次 `python3 scripts/query_flow.py` 观察第二次枚举走缓存）**
- [ ] **Step 5: change-log + 提交** `✨ feat(query): 组件枚举本地缓存（TTL 24h），超时降级读缓存（双份同步）`
