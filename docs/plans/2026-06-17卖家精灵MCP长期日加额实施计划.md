# 卖家精灵MCP长期日加额 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `seller_sprite_run` 增加“默认额度 + 按邮箱长期日加额”的运行时能力，并提供不对外暴露的内部 SQL 运维手册。

**Architecture:** 现有 `mcp_quota_daily` 继续只记录每日使用结果；新增 `mcp_quota_bonus_daily` 保存 `service + email` 维度的长期日加额。`QuotaLimiter` 在限额判断前统一解析邮箱，读取基础额度与 bonus 额度后计算实际日额度。

**Tech Stack:** Python 3.10、Typer、SQLite3、pytest

---

## 文件结构

- 修改 `opscli/mcp/quota.py`
  - 将限额主身份收敛为 `email`
  - 在 SQLite schema 中新增 `mcp_quota_bonus_daily`
  - 增加 bonus 表读取逻辑
  - 让返回的 `quota.limit` 反映实际额度
- 修改 `tests/mcp/test_quota.py`
  - 新增邮箱主匹配、bonus 表持久化、实际额度计算等回归测试
- 新增 `docs/guide/卖家精灵MCP长期日加额内部SQL手册.md`
  - 只作为仓库内部手册，提供建表、upsert、查询、删除 SQL
- 修改 `docs/change-log-pending.md`
  - 追加本次代码修改记录

### Task 1: 先用测试锁定邮箱主匹配和长期日加额行为

**Files:**
- Modify: `tests/mcp/test_quota.py`
- Test: `tests/mcp/test_quota.py`

- [ ] **Step 1: 写邮箱主匹配的失败测试**

```python
def test_identity_resolver_prefers_email_then_api_key_hash():
    token = mcp_request_ctx.set({
        "api_key": "raw-api-key",
        "user_id": "user-1",
        "email": "USER@example.com",
    })
    try:
        assert QuotaIdentityResolver().resolve() == "email:user@example.com"
    finally:
        mcp_request_ctx.reset(token)
```

- [ ] **Step 2: 运行定向测试确认 RED**

Run: `uv run --extra dev pytest tests/mcp/test_quota.py::test_identity_resolver_prefers_email_then_api_key_hash -q`

Expected: FAIL，原因是当前实现仍优先返回 `user:user-1`

- [ ] **Step 3: 写长期日加额读取的失败测试**

```python
def test_sqlite_quota_store_applies_bonus_daily_limit(tmp_path):
    db_path = tmp_path / "quota.sqlite3"
    policy = QuotaPolicy(tool_name="seller_sprite_run", service="seller_sprite", daily_limit=5)
    store = SQLiteQuotaStore(db_path)

    _run(store.upsert_bonus_daily_limit("seller_sprite", "user@example.com", 3))

    for _ in range(8):
        allowed, snapshot = _run(store.reserve(policy, "email:user@example.com"))
        assert allowed is True

    blocked, blocked_snapshot = _run(store.reserve(policy, "email:user@example.com"))

    assert snapshot["limit"] == 8
    assert blocked is False
    assert blocked_snapshot["used"] == 8
    assert blocked_snapshot["remaining"] == 0
```

- [ ] **Step 4: 运行定向测试确认 RED**

Run: `uv run --extra dev pytest tests/mcp/test_quota.py::test_sqlite_quota_store_applies_bonus_daily_limit -q`

Expected: FAIL，原因是当前 `SQLiteQuotaStore` 没有 `upsert_bonus_daily_limit()`，且 `reserve()` 仍只认基础额度 `5`

- [ ] **Step 5: 写邮箱缺失时回退本地凭证的失败测试**

```python
def test_identity_resolver_falls_back_to_local_credential_email(monkeypatch):
    monkeypatch.setattr("opscli.mcp.quota.get_current_user_email", lambda: None)
    monkeypatch.setattr("opscli.mcp.quota.get_current_api_key", lambda: "raw-api-key")
    monkeypatch.setattr("opscli.mcp.quota._load_local_quota_email", lambda: "local@example.com")

    assert QuotaIdentityResolver().resolve() == "email:local@example.com"
```

- [ ] **Step 6: 运行定向测试确认 RED**

Run: `uv run --extra dev pytest tests/mcp/test_quota.py::test_identity_resolver_falls_back_to_local_credential_email -q`

Expected: FAIL，原因是当前实现没有本地凭证邮箱回退函数

- [ ] **Step 7: 提交测试阶段**

```bash
git add tests/mcp/test_quota.py
git commit -m "test: 锁定长期日加额行为"
```

### Task 2: 最小实现 quota bonus 表与邮箱主匹配

**Files:**
- Modify: `opscli/mcp/quota.py`
- Test: `tests/mcp/test_quota.py`

- [ ] **Step 1: 增加本地凭证邮箱回退 helper**

```python
def _load_local_quota_email() -> str | None:
    from opscli.auth.storage.credential_store import CredentialStore
    from opscli.mcp.tools.helpers import _get_credential_dir

    try:
        cred_dir = _get_credential_dir()
        store = CredentialStore(base_dir=cred_dir) if cred_dir else CredentialStore()
        data = store.load()
    except Exception:
        return None
    email = data.get("email") if data else None
    if not email:
        return None
    return str(email).strip().lower()
```

- [ ] **Step 2: 将身份解析器改为优先邮箱**

```python
class QuotaIdentityResolver:
    def resolve(self) -> str | None:
        email = get_current_user_email()
        if email:
            return f"email:{email.strip().lower()}"

        local_email = _load_local_quota_email()
        if local_email:
            return f"email:{local_email}"

        api_key = get_current_api_key()
        if api_key:
            return f"api_key:{_hash_api_key(api_key)}"
        return None
```

- [ ] **Step 3: 在 SQLite schema 中新增 bonus 表**

```python
conn.execute(
    """
    CREATE TABLE IF NOT EXISTS mcp_quota_bonus_daily (
        service TEXT NOT NULL,
        email TEXT NOT NULL,
        bonus_daily_limit INTEGER NOT NULL DEFAULT 0,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        PRIMARY KEY (service, email)
    )
    """
)
```

- [ ] **Step 4: 增加 bonus 表的最小读写接口**

```python
async def upsert_bonus_daily_limit(self, service: str, email: str, bonus_daily_limit: int) -> None:
    normalized_email = email.strip().lower()
    with self._connect() as conn:
        conn.execute(
            """
            INSERT INTO mcp_quota_bonus_daily (
                service, email, bonus_daily_limit, created_at, updated_at
            )
            VALUES (?, ?, ?, ?, ?)
            ON CONFLICT(service, email) DO UPDATE SET
                bonus_daily_limit = excluded.bonus_daily_limit,
                updated_at = excluded.updated_at
            """,
            (
                service,
                normalized_email,
                bonus_daily_limit,
                _updated_at_iso(datetime.now(UTC)),
                _updated_at_iso(datetime.now(UTC)),
            ),
        )
```

- [ ] **Step 5: 在 reserve 中读取实际额度**

```python
bonus_limit = self._load_bonus_daily_limit(conn, policy.service, identity_type, identity_key)
effective_limit = policy.daily_limit + bonus_limit

if calls >= effective_limit:
    conn.commit()
    return False, _snapshot(policy, effective_limit, calls, failures, now)

calls += 1
self._update_record(conn, policy, identity_key, identity_hash, calls, failures, effective_limit, now)
conn.commit()
return True, _snapshot(policy, effective_limit, calls, failures, now)
```

- [ ] **Step 6: 让失败退回逻辑和快照使用实际额度**

```python
bonus_limit = self._load_bonus_daily_limit(conn, policy.service, identity_type, identity_key)
effective_limit = policy.daily_limit + bonus_limit
calls = max(calls - 1, 0)
failures += 1
self._update_record(conn, policy, identity_key, identity_hash, calls, failures, effective_limit, now)
return _snapshot(policy, effective_limit, calls, failures, now)
```

- [ ] **Step 7: 运行 quota 定向测试确认 GREEN**

Run: `uv run --extra dev pytest tests/mcp/test_quota.py -q`

Expected: PASS，新增邮箱主匹配、bonus 表和额度快照测试全部通过

- [ ] **Step 8: 提交最小实现**

```bash
git add opscli/mcp/quota.py tests/mcp/test_quota.py
git commit -m "feat: 支持卖家精灵长期日加额"
```

### Task 3: 补内部 SQL 手册、变更记录和回归验证

**Files:**
- Create: `docs/guide/卖家精灵MCP长期日加额内部SQL手册.md`
- Modify: `docs/change-log-pending.md`
- Test: `tests/mcp/test_seller_sprite_tools.py`

- [ ] **Step 1: 写内部 SQL 手册**

````markdown
# 卖家精灵MCP长期日加额内部SQL手册

## 适用范围

- 仅用于内部运维
- 不对普通用户公开
- 只适用于 `seller_sprite`

## 建表 SQL

```sql
CREATE TABLE IF NOT EXISTS mcp_quota_bonus_daily (
    service TEXT NOT NULL,
    email TEXT NOT NULL,
    bonus_daily_limit INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    PRIMARY KEY (service, email)
);
```
````

- [ ] **Step 2: 追加变更记录**

````markdown
## 2026-06-17 MCP - 卖家精灵长期日加额

**变更原因**：需要在默认日限额基础上，按邮箱为指定用户长期追加卖家精灵 MCP 每日额度，同时避免新增公开运维命令。
**改动点**：`opscli/mcp/quota.py` 改为以邮箱作为限额主身份并支持本地凭证邮箱回退；SQLite 新增 `mcp_quota_bonus_daily` 长期日加额表；`tests/mcp/test_quota.py` 增加长期日加额和邮箱主匹配回归测试；新增内部 SQL 手册。
**验证结果**：`uv run --extra dev pytest tests/mcp/test_quota.py tests/mcp/test_seller_sprite_tools.py -q`
**影响范围**：影响 `seller_sprite_run` 的限额判定口径和 SQLite schema。
**回滚方式**：回退 `opscli/mcp/quota.py`、删除 `mcp_quota_bonus_daily` 表和对应测试、移除内部 SQL 手册。
---
````

- [ ] **Step 3: 运行卖家精灵和 MCP 回归**

Run: `uv run --extra dev pytest tests/mcp/test_quota.py tests/mcp/test_seller_sprite_tools.py tests/mcp/test_tools.py -q`

Expected: PASS，配额切面和工具注册链路保持绿色

- [ ] **Step 4: 运行编译检查**

Run: `python -m py_compile opscli/mcp/quota.py`

Expected: PASS，无语法错误

- [ ] **Step 5: 提交文档与记录**

```bash
git add docs/guide/卖家精灵MCP长期日加额内部SQL手册.md docs/change-log-pending.md
git commit -m "docs: 补充长期日加额运维手册"
```

## 自检结果

- 规格覆盖：已覆盖邮箱主匹配、bonus 表、默认额度叠加、内部 SQL 手册、无公开 CLI 命令等要求
- 占位符检查：无 `TODO`、`TBD`、模糊“后续补充”类步骤
- 类型一致性：计划中统一使用 `service + email + bonus_daily_limit` 作为长期日加额模型
