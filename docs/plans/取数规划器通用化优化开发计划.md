# 取数规划器通用化优化开发计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 ops-dataset-query 的选表选字段从「人工维护 16 条意图 + 15 份画像」转为「元数据驱动 + 确定性补全规则 + 薄人工语义层」，使新增数据集无需人工登记即可被正确路由。

**Architecture:** 主规划器已由 `scoped_metadata_index.build_cards()` 从三张 CSV 自动生成决策卡片，本计划不重写选表框架，只做三件事：① 在字段打分链路末端补一层纯标准库的确定性规则（字符二元组模糊匹配 + 公式引用词表扩展），解决「业务词匹配不到字段」导致的零候选；② 把回归评测从单层（选中哪个数据集）拆成三层（数据集 / 字段严格召回 / 端到端），使后续改动可度量；③ 给 `dataset_profiles.json` 瘦身并按 `dataset_alias` 索引，只保留无法从元数据推导的人工语义。

**Tech Stack:** Python ≥3.10 纯标准库（Skill 侧脚本禁止第三方依赖）；pytest；两版规划器需保持行为一致（Skill 版 `opscli/skills/templates/ops-dataset-query/scripts/`、内核版 `opscli/query/services/planner/`）。

## Global Constraints

- Skill 侧脚本只允许 Python 标准库，禁止引入第三方依赖，必须可离线运行（见 SKILL.md 与项目 CLAUDE.md）。
- 所有代码注释与 docstring 使用中文；关键业务逻辑必须写明「为什么这样做」（项目【铁律17】）。
- 终端输出字符必须 GBK 兼容：禁止 `✓ ✗ ⚠ ✅ ❌` 等，成功用 `√`、失败用 `×`（项目【铁律23】）。
- 两版规划器同源：任何行为改动必须同时落到 Skill 版与内核版，并由参数化测试逐条对齐（现有 `tests/skills/test_component_filter_resolution.py` 即此模式）。
- 每次代码修改后必须把改动追加到 `docs/change-log-pending.md`（项目【铁律18】）。
- 改完必须同步到四个安装目录：`~/.opscli/skills`、`~/.codex/skills`、`~/.claude/skills`、`~/.openclaw/skills`。
- 提交信息用中文 conventional commit，无 emoji 前缀（匹配现有 git 历史）；不主动 `git push`。
- 禁止降低召回换精度：研究结论为「漏一个必需字段等于必然失败，多带无关字段代价小得多」，澄清阈值设在「召回不确定」而非「精度不够」。

## File Structure

**新建：**
- `opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py` — 纯确定性补全规则（字符二元组相似度、公式引用词表提取）。无第三方依赖，不发起任何网络调用。
- `opscli/query/services/planner/schema_completion.py` — 上一文件的内核版镜像，内容逐字相同（两版同源）。
- `tests/skills/test_schema_completion.py` — 补全规则单测，对两版参数化。
- `tests/skills/test_routing_eval.py` — 三层路由评测（数据集 / 字段严格召回 / 端到端）。
- `scripts/` 下不新增其它文件；巡检能力挂到已有的 `local_fallback.py` 子命令。

**修改：**
- `.../scripts/dataset_guidance.py` — `_field_score()` 末端接入模糊匹配（Skill 版）。
- `opscli/query/services/planner/dataset_guidance.py` — 同上（内核版）。
- `.../scripts/scoped_metadata_index.py` 与内核版同名文件 — `_build_cards()` 把公式引用字段并入 `metric_terms`。
- `.../data/dataset_profiles.json` — 按 `dataset_alias` 索引、删除可派生字段、新增 `certified` 状态位。
- `.../scripts/local_fallback.py` — 适配新画像结构，新增 `--audit` 覆盖率巡检。
- `tests/skills/data/routing_eval_cases.json` — 每条用例补 `expected_fields`。
- `tests/skills/test_local_fallback.py` — 适配新画像结构。

---

## 阶段一（P0）：确定性补全规则

> 研究依据：EDBT'26《In-depth Analysis of LLM-based Schema Linking》— 四条纯算法规则在 BIRD 上把 schema-linking 召回从 43.07 提到 69.40（+26.33），精度只掉 4.75。本阶段实现其中适用于本项目的两条，全部纯标准库。
> 实测依据：查询「看一下搜索词的点击份额和购买份额」当前返回**零候选**，因为 `_field_score` 的兜底是 token 集合交集，无任何模糊匹配。

### Task 1: 字符二元组相似度模块（Skill 版）

**Files:**
- Create: `opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py`
- Test: `tests/skills/test_schema_completion.py`

**Interfaces:**
- Consumes: 无（纯函数模块，只用标准库）
- Produces:
  - `bigram_similarity(left: str, right: str) -> float` — 返回 0.0~1.0
  - `FUZZY_MATCH_THRESHOLD: float = 0.5`
  - `best_fuzzy_score(query_text: str, labels: list[str]) -> float`

- [ ] **Step 1: 写失败的测试**

创建 `tests/skills/test_schema_completion.py`：

```python
"""确定性补全规则单测：字符二元组相似度与公式引用词表提取。

为什么需要：主规划器的字段打分末端只有 token 集合交集，
「点击份额」这类与真实字段中文名部分重叠的业务词会直接落空，
实测返回零候选。本模块补的是纯算法、无网络、无第三方依赖的一层。
"""

from __future__ import annotations

import sys
from pathlib import Path

import pytest

SCRIPTS_DIR = (
    Path(__file__).parents[2]
    / "opscli"
    / "skills"
    / "templates"
    / "ops-dataset-query"
    / "scripts"
)
if str(SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPTS_DIR))

import schema_completion as skill_completion  # noqa: E402


def test_identical_text_scores_one():
    assert skill_completion.bigram_similarity("点击份额", "点击份额") == 1.0


def test_unrelated_text_scores_zero():
    assert skill_completion.bigram_similarity("点击份额", "库存金额") == 0.0


def test_partial_overlap_scores_between():
    score = skill_completion.bigram_similarity("点击份额", "购买份额")
    assert 0.0 < score < 1.0


def test_short_text_is_not_matched():
    """单字与空串不参与模糊匹配，否则任意查询都会命中一堆字段。"""
    assert skill_completion.bigram_similarity("点", "点击份额") == 0.0
    assert skill_completion.bigram_similarity("", "点击份额") == 0.0
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/mask/python3/opscli && source .venv/bin/activate
python -m pytest tests/skills/test_schema_completion.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'schema_completion'`

- [ ] **Step 3: 写最小实现**

创建 `opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py`：

```python
#!/usr/bin/env python3
"""确定性 schema 补全规则：纯标准库、无网络、可离线。

为什么需要：主规划器的字段打分末端是 token 集合交集，业务词与字段
中文名只是部分重叠时（「点击份额」对「购买份额」）会直接得 0 分，
整条查询因此零候选。本模块提供字符二元组相似度作为最后一层兜底，
按「召回优先于精确率」的原则设计——宁可多带候选交由澄清，
也不要因为匹配不上而完全没有候选。

阈值 0.5 取自 EDBT'26 schema linking 论文的近似字符串匹配规则。
"""

from __future__ import annotations

import unicodedata

# 低于该相似度不视为命中：0.5 以下基本是偶然共享一两个字，
# 放进候选只会稀释排序而不会提升召回
FUZZY_MATCH_THRESHOLD = 0.5
# 少于两个字符无法构成二元组，直接判不匹配，避免单字命中一片字段
MIN_FUZZY_LENGTH = 2


def _normalize(value: object) -> str:
    """NFKC + casefold + 去首尾空白，与 dataset_guidance 的归一化口径一致。"""
    if not isinstance(value, str):
        return ""
    return unicodedata.normalize("NFKC", value).casefold().strip()


def _bigrams(text: str) -> set[str]:
    """切成相邻字符二元组集合，中文按字切、英文按字符切。"""
    return {text[index : index + 2] for index in range(len(text) - 1)}


def bigram_similarity(left: str, right: str) -> float:
    """字符二元组 Jaccard 相似度，返回 0.0~1.0。

    选二元组而不是编辑距离：中文业务词的差异通常是整词替换
    （「点击份额」/「购买份额」），二元组能保住共享的「份额」，
    而编辑距离会把它算成一半不同。
    """
    left_text, right_text = _normalize(left), _normalize(right)
    if len(left_text) < MIN_FUZZY_LENGTH or len(right_text) < MIN_FUZZY_LENGTH:
        return 0.0
    left_grams, right_grams = _bigrams(left_text), _bigrams(right_text)
    if not left_grams or not right_grams:
        return 0.0
    intersection = len(left_grams & right_grams)
    if not intersection:
        return 0.0
    return intersection / len(left_grams | right_grams)


def best_fuzzy_score(query_text: str, labels: list) -> float:
    """取查询文本与一组字段标签的最高相似度，低于阈值归零。"""
    best = 0.0
    for label in labels:
        score = bigram_similarity(query_text, label)
        if score > best:
            best = score
    return best if best >= FUZZY_MATCH_THRESHOLD else 0.0
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_schema_completion.py -v
```

Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py tests/skills/test_schema_completion.py
git commit -m "feat(planner): 新增字符二元组相似度模块用于字段模糊匹配"
```

---

### Task 2: 字段打分接入模糊匹配（Skill 版）

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/dataset_guidance.py`（`_field_score` 函数）
- Test: `tests/skills/test_schema_completion.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `schema_completion.best_fuzzy_score(query_text, labels) -> float`
- Produces: `_field_score()` 在无精确命中时返回 `1..50` 的模糊分（低于所有精确命中档位）

- [ ] **Step 1: 写失败的测试**

追加到 `tests/skills/test_schema_completion.py`：

```python
import dataset_guidance as skill_guidance  # noqa: E402


def _field(field_name: str, verbose_name: str) -> dict:
    return {
        "field_name": field_name,
        "verbose_name": verbose_name,
        "field_type": "metric",
        "dataset_alias": "ds_test",
    }


def test_fuzzy_match_rescues_zero_score_field():
    """业务词与字段中文名部分重叠时不能再得 0 分。

    实测形态：「搜索词的点击份额和购买份额」在当前实现下零候选，
    因为打分末端只有 token 交集。
    """
    field = _field("buy_share", "购买份额")
    score = skill_guidance._field_score(
        field, "看一下搜索词的点击份额", {"点击", "份额", "搜索"}
    )
    assert score > 0


def test_fuzzy_score_never_outranks_exact_match():
    """模糊分必须低于任何精确命中，否则会把准确字段挤下去。"""
    exact = skill_guidance._field_score(
        _field("buy_share", "购买份额"), "查购买份额", {"购买", "份额"}
    )
    fuzzy = skill_guidance._field_score(
        _field("buy_share", "购买份额"), "查点击份额", {"点击", "份额"}
    )
    assert fuzzy < exact


def test_unrelated_field_stays_zero():
    """完全无关的字段不能因为模糊匹配被拉进候选。"""
    score = skill_guidance._field_score(
        _field("stock_qty", "总库存"), "查点击份额", {"点击", "份额"}
    )
    assert score == 0
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/skills/test_schema_completion.py -k fuzzy -v
```

Expected: `test_fuzzy_match_rescues_zero_score_field` FAIL（返回 0）

- [ ] **Step 3: 写最小实现**

在 `dataset_guidance.py` 顶部导入区加入（与其它同级模块导入并列）：

```python
import schema_completion
```

把 `_field_score` 的末尾改为（原 `best = max(best, score)` 之后、`return best` 之前）：

```python
        best = max(best, score)
    if best:
        return best
    # 精确与 token 交集都落空时的最后一层：字符二元组模糊匹配。
    # 上限压到 50，确保任何精确命中（80/90/100 档）都排在它前面；
    # 这条规则的目的是不让「点击份额」这类业务词零候选，
    # 而不是替代精确匹配。
    fuzzy = schema_completion.best_fuzzy_score(
        normalized_query, [_normalize(label) for label in _field_labels(field)]
    )
    return int(fuzzy * 50)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_schema_completion.py -v
python -m pytest tests/skills/test_dataset_query_planner.py tests/skills/test_dataset_guidance_default_filters.py -q
```

Expected: 新测试全部 passed；既有规划器测试无回归

- [ ] **Step 5: 实测验证零候选被修复**

```bash
cd ~/.claude/skills/ops-dataset-query/scripts
cp /Users/mask/python3/opscli/opscli/skills/templates/ops-dataset-query/scripts/{schema_completion.py,dataset_guidance.py} .
rm -rf __pycache__
/bin/zsh -lc "cd $PWD && python3 query_plan.py '看一下搜索词的点击份额和购买份额' --no-auto-enum 2>/dev/null" | python3 -c "
import json,sys
d=json.load(sys.stdin); mv=d.get('model_view') or {}
print('status=',d.get('status'),'| 选中=',mv.get('dataset_name_zh'),'| 候选=',[c.get('name_zh') for c in (mv.get('dataset_candidates_zh') or [])])"
```

Expected: 不再是空候选（选中或列出候选皆可，零候选即未达成）

- [ ] **Step 6: 提交**

```bash
cd /Users/mask/python3/opscli
git add opscli/skills/templates/ops-dataset-query/scripts/dataset_guidance.py tests/skills/test_schema_completion.py
git commit -m "feat(planner): 字段打分末端接入模糊匹配，修复业务词零候选"
```

---

### Task 3: 公式引用字段并入卡片词表（Skill 版）

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py`（新增函数）
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/scoped_metadata_index.py`（`_build_cards`）
- Test: `tests/skills/test_schema_completion.py`（追加）

**Interfaces:**
- Consumes: Task 1 的模块
- Produces: `formula_reference_terms(expression: str, field_labels: dict) -> list[str]`
  - `field_labels`：`{field_name: verbose_name}` 映射
  - 返回表达式中引用到的字段的中文名列表（去重、排序）

- [ ] **Step 1: 写失败的测试**

追加到 `tests/skills/test_schema_completion.py`：

```python
def test_formula_reference_terms_extracts_chinese_labels():
    """ACOS 的公式引用了广告费与销售额，这两个中文名应能匹配到 ACOS 所在数据集。

    否则用户说「广告费」时，只有直接叫广告费的字段所在数据集会进候选，
    真正能算 ACOS 的数据集反而漏掉。
    """
    labels = {"ads_cost": "广告费", "sales": "销售额", "stock_qty": "总库存"}
    terms = skill_completion.formula_reference_terms("ads_cost / sales", labels)
    assert terms == ["广告费", "销售额"]


def test_formula_reference_terms_ignores_unknown_identifiers():
    """表达式里的函数名与未知标识符不得被当成字段。"""
    labels = {"ads_cost": "广告费"}
    assert skill_completion.formula_reference_terms("SUM(ads_cost) / COUNT(x)", labels) == ["广告费"]


def test_formula_reference_terms_handles_empty_expression():
    assert skill_completion.formula_reference_terms("", {"a": "甲"}) == []
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/skills/test_schema_completion.py -k formula -v
```

Expected: FAIL，`AttributeError: module 'schema_completion' has no attribute 'formula_reference_terms'`

- [ ] **Step 3: 写最小实现**

在 `schema_completion.py` 追加：

```python
import re

# 公式表达式里的字段引用形态：字母开头的标识符（含下划线与数字）
_IDENTIFIER_RE = re.compile(r"[A-Za-z_][A-Za-z0-9_]*")


def formula_reference_terms(expression: object, field_labels: dict) -> list:
    """提取公式表达式引用到的字段中文名。

    为什么需要：ACOS 的公式是 ads_cost / sales，用户说「广告费」时
    这个指标所在的数据集本该进候选，但卡片词表里只有「ACOS」三个字，
    匹配不上。把公式引用的底层字段中文名并入词表即可自动关联，
    无需人工在画像里登记「这个数据集能算广告相关指标」。

    未知标识符（SUM/COUNT 等函数名）不在授权字段映射里，自动被忽略。
    """
    if not isinstance(expression, str) or not expression.strip():
        return []
    referenced = {
        field_labels[name]
        for name in _IDENTIFIER_RE.findall(expression)
        if name in field_labels and field_labels[name]
    }
    return sorted(referenced)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_schema_completion.py -k formula -v
```

Expected: 3 passed

- [ ] **Step 5: 接入卡片构建**

在 `scoped_metadata_index.py` 顶部导入区加入：

```python
import schema_completion
```

在 `_build_cards()` 里，把 `metric_terms` 的构造替换为：

```python
    # 公式指标的底层引用字段中文名并入词表：用户说「广告费」时，
    # 靠公式关联的 ACOS 所在数据集也应进候选，无需人工登记
    field_labels = {
        row["field_name"]: _clean(row.get("verbose_name"))
        for row in fields
        if row.get("field_name")
    }
```

（放在 `cards = []` 之前）；随后把卡片字典里的 `metric_terms` 一项改为：

```python
                "metric_terms": _sorted_terms(
                    fields_by_alias[alias]["metric"],
                    ("field_name", "verbose_name"),
                )
                + [
                    term
                    for row in fields_by_alias[alias]["metric"]
                    for term in schema_completion.formula_reference_terms(
                        row.get("summary_expression") or row.get("detail_expression"),
                        field_labels,
                    )
                ],
```

- [ ] **Step 6: 运行全部规划器测试**

```bash
python -m pytest tests/skills/test_dataset_query_planner.py tests/skills/test_schema_completion.py tests/query/planner -q
```

Expected: 全部 passed（卡片词表变长不应改变既有断言）

- [ ] **Step 7: 提交**

```bash
git add opscli/skills/templates/ops-dataset-query/scripts/{schema_completion.py,scoped_metadata_index.py} tests/skills/test_schema_completion.py
git commit -m "feat(planner): 公式引用字段中文名并入卡片词表"
```

---

### Task 4: 镜像到内核版并锁定两版一致

**Files:**
- Create: `opscli/query/services/planner/schema_completion.py`
- Modify: `opscli/query/services/planner/dataset_guidance.py`
- Modify: `opscli/query/services/planner/scoped_metadata_index.py`
- Test: `tests/skills/test_schema_completion.py`（改为对两版参数化）

**Interfaces:**
- Consumes: Task 1-3 的 Skill 版实现
- Produces: 内核版同名模块与函数，签名逐字相同

- [ ] **Step 1: 把测试改为两版参数化**

在 `tests/skills/test_schema_completion.py` 的导入区后追加，并把已有测试函数的 `skill_completion` 全部替换为参数 `module`：

```python
from opscli.query.services.planner import schema_completion as kernel_completion  # noqa: E402

BOTH_VERSIONS = pytest.mark.parametrize(
    "module",
    [skill_completion, kernel_completion],
    ids=["skill", "kernel"],
)
```

给每个 `test_*` 函数加上 `@BOTH_VERSIONS` 装饰器与 `module` 形参，例如：

```python
@BOTH_VERSIONS
def test_identical_text_scores_one(module):
    assert module.bigram_similarity("点击份额", "点击份额") == 1.0
```

- [ ] **Step 2: 运行测试确认内核版失败**

```bash
python -m pytest tests/skills/test_schema_completion.py -v
```

Expected: 所有 `[kernel]` 用例 FAIL（模块不存在）

- [ ] **Step 3: 复制模块并接入内核版**

```bash
cd /Users/mask/python3/opscli
cp opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py \
   opscli/query/services/planner/schema_completion.py
```

内核版 `opscli/query/services/planner/dataset_guidance.py` 顶部导入区加入：

```python
from opscli.query.services.planner import schema_completion
```

把该文件 `_field_score()` 的 `best = max(best, score)` 之后、`return best` 之前改为：

```python
        best = max(best, score)
    if best:
        return best
    # 精确与 token 交集都落空时的最后一层：字符二元组模糊匹配。
    # 上限压到 50，确保任何精确命中（80/90/100 档）都排在它前面；
    # 这条规则的目的是不让「点击份额」这类业务词零候选，
    # 而不是替代精确匹配。
    fuzzy = schema_completion.best_fuzzy_score(
        normalized_query, [_normalize(label) for label in _field_labels(field)]
    )
    return int(fuzzy * 50)
```

内核版 `opscli/query/services/planner/scoped_metadata_index.py` 顶部导入区加入同一行导入；在 `_build_cards()` 的 `cards = []` 之前插入：

```python
    # 公式指标的底层引用字段中文名并入词表：用户说「广告费」时，
    # 靠公式关联的 ACOS 所在数据集也应进候选，无需人工登记
    field_labels = {
        row["field_name"]: _clean(row.get("verbose_name"))
        for row in fields
        if row.get("field_name")
    }
```

并把卡片字典里的 `metric_terms` 一项改为：

```python
                "metric_terms": _sorted_terms(
                    fields_by_alias[alias]["metric"],
                    ("field_name", "verbose_name"),
                )
                + [
                    term
                    for row in fields_by_alias[alias]["metric"]
                    for term in schema_completion.formula_reference_terms(
                        row.get("summary_expression") or row.get("detail_expression"),
                        field_labels,
                    )
                ],
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_schema_completion.py tests/query/planner tests/skills/test_dataset_query_planner.py -q
```

Expected: 全部 passed，`[skill]` 与 `[kernel]` 一一对应

- [ ] **Step 5: 同步四个安装目录并记录变更**

```bash
for d in ~/.opscli/skills ~/.codex/skills ~/.claude/skills ~/.openclaw/skills; do
  [ -d "$d/ops-dataset-query" ] && cp opscli/skills/templates/ops-dataset-query/scripts/*.py "$d/ops-dataset-query/scripts/" && rm -rf "$d/ops-dataset-query/scripts/__pycache__"
done
```

按【铁律18】把本阶段改动追加到 `docs/change-log-pending.md`，格式参照该文件既有条目（变更原因 / 改动点 / 验证结果 / 影响范围 / 回滚方式）。

- [ ] **Step 6: 提交**

```bash
git add opscli/query/services/planner/ tests/skills/test_schema_completion.py docs/change-log-pending.md
git commit -m "feat(planner): 补全规则镜像到内核版并锁定两版一致"
```

---

## 阶段二（P0.5）：路由质量三层度量

> 研究依据：EDBT'26 与 AutoLink 都把「严格 schema linking 召回」（所有必需字段都被选中的样本占比）作为独立于端到端准确率的一级指标。现有 21 条回归只测「选中哪个数据集」，无法回答阶段一是否真的提升了字段召回。
> 注意：研究中关于「厂商用线上点赞点踩构成回归闭环」的说法在对抗验证中被全票否决，本阶段不引入该做法。

### Task 5: 评测用例补充字段期望

**Files:**
- Modify: `tests/skills/data/routing_eval_cases.json`
- Test: `tests/skills/test_routing_eval.py`（新建）

**Interfaces:**
- Consumes: `local_fallback.build_fallback()` 与 `query_plan.build_model_query_plan()`
- Produces: 每条用例新增 `expected_fields: list[str]`（字段中文名，可为空数组表示不校验）

- [ ] **Step 1: 给用例补字段期望**

编辑 `tests/skills/data/routing_eval_cases.json`。先用脚本给全部 21 条补空数组，保证结构一致：

```bash
cd /Users/mask/python3/opscli && source .venv/bin/activate && python3 - << 'PY'
import json, pathlib
path = pathlib.Path("tests/skills/data/routing_eval_cases.json")
payload = json.loads(path.read_text(encoding="utf-8"))
for case in payload["cases"]:
    case.setdefault("expected_fields", [])
path.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
print(f"已补 expected_fields 字段，共 {len(payload['cases'])} 条")
PY
```

然后手工把下面 5 条填上真实期望（字段中文名须与 `dataset_fields.csv` 的 `verbose_name` 逐字一致，可用 `grep 'ASIN' ~/.claude/skills/ops-dataset-query/data/dataset_fields.csv` 核对）：

| case id | user_query | expected_fields |
|---|---|---|
| case_001 | 看一下近30天整体经营情况，销售广告库存流量一起看 | `["销售额", "广告费", "流量", "总库存"]` |
| case_004 | 看一下各平台广告整体投入和ACOS | `["平台", "广告费", "ACOS"]` |
| case_014 | ASIN流量和转化率 | `["ASIN", "流量", "转化率"]` |
| case_018 | 物控库龄超180天库存金额 | `["库龄", "库存金额"]` |
| case_020 | Deal和Coupons活动售出量 | `["活动类型", "售出量"]` |

若核对时发现某个中文名在当前元数据中不存在，把该条的 `expected_fields` 留空并在 `note` 里写明原因，不要凭猜填写。

- [ ] **Step 2: 写失败的测试**

创建 `tests/skills/test_routing_eval.py`：

```python
"""路由质量三层评测：数据集正确性、字段严格召回、可执行性。

为什么拆三层：研究结论是「漏一个必需字段就必然失败」，
而端到端成功率会把选表错误与字段漏选混在一起，改动无法归因。
"""

from __future__ import annotations

import json
import sys
from pathlib import Path

import pytest

SKILL_ROOT = (
    Path(__file__).parents[2] / "opscli" / "skills" / "templates" / "ops-dataset-query"
)
SCRIPTS_DIR = SKILL_ROOT / "scripts"
if str(SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPTS_DIR))

import query_plan as skill_query_plan  # noqa: E402

EVAL_CASES = json.loads(
    (Path(__file__).parent / "data" / "routing_eval_cases.json").read_text(encoding="utf-8")
)["cases"]

FIELD_CASES = [case for case in EVAL_CASES if case.get("expected_fields")]


@pytest.mark.parametrize("case", FIELD_CASES, ids=[c["id"] for c in FIELD_CASES])
def test_strict_field_recall(case: dict):
    """严格召回：期望字段必须全部出现在规划结果里，漏一个即失败。"""
    result = skill_query_plan.build_model_query_plan(
        case["user_query"], auto_upgrade=False, auto_enum=False
    )
    view = result.get("model_view") or {}
    selected = set(view.get("dimensions") or []) | set(view.get("metrics") or [])
    missing = [name for name in case["expected_fields"] if name not in selected]
    assert not missing, f"{case['id']} 漏选字段 {missing}，实际选中 {sorted(selected)}"
```

- [ ] **Step 3: 运行测试建立基线**

```bash
python -m pytest tests/skills/test_routing_eval.py -v
```

Expected: 部分 FAIL 属正常——这是**基线**，把当前失败的 case id 记录到测试文件顶部 docstring，作为阶段一效果的对照。不要为了让测试变绿而修改期望值。

- [ ] **Step 4: 把仍失败的用例标记为 xfail 并写明成因**

对每条仍失败的用例，按现有 `tests/skills/test_local_fallback.py` 的 `KNOWN_ROUTING_GAPS` 模式加 `strict xfail` 与中文成因说明。禁止直接删用例或改期望。

- [ ] **Step 5: 运行确认全绿（含 xfail）**

```bash
python -m pytest tests/skills/test_routing_eval.py -q
```

Expected: `N passed, M xfailed`，无 failed

- [ ] **Step 6: 提交**

```bash
git add tests/skills/test_routing_eval.py tests/skills/data/routing_eval_cases.json
git commit -m "test(planner): 路由评测拆为数据集与字段严格召回两层"
```

---

## 阶段三（P1）：画像瘦身与止腐

> 实测依据：后端 60 个数据集，画像覆盖 15 个，其中 5 个指向的数据集在后端已改名或消失——文件按中文名索引，改名即腐烂。
> 研究依据：可自动派生的部分（默认维度指标、字段清单）应实时生成；只保留人工审核过的薄层（业务口径、禁用场景），并给它 review/certify 状态位。

### Task 6: 画像改为按 alias 索引并删除可派生字段

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/data/dataset_profiles.json`
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/local_fallback.py`
- Test: `tests/skills/test_local_fallback.py`

**Interfaces:**
- Consumes: 现有 `local_fallback._load_profiles()` / `_dataset_candidates()`
- Produces: 画像条目新结构 `{dataset_alias, standard_name, hard_constraints, clarify_when, avoid_when, certified, reviewed_at}`；不再含 `default_dimensions` / `default_metrics` / `table_id`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/skills/test_local_fallback.py`：

```python
def test_profiles_are_indexed_by_alias(tmp_path: Path):
    """画像必须按 dataset_alias 索引：按中文名索引会在数据集改名时静默腐烂。

    实测：现有 15 份画像里有 5 份的 standard_name 已对不上后端。
    """
    profiles = json.loads(
        (SKILL_ROOT / "data" / "dataset_profiles.json").read_text(encoding="utf-8")
    )
    for item in profiles["datasets"]:
        assert item.get("dataset_alias"), f"画像缺少 dataset_alias：{item.get('standard_name')}"


def test_profiles_carry_certification_state(tmp_path: Path):
    """每份画像必须带审核状态，未审核的只能作提示不能作硬约束。"""
    profiles = json.loads(
        (SKILL_ROOT / "data" / "dataset_profiles.json").read_text(encoding="utf-8")
    )
    for item in profiles["datasets"]:
        assert item.get("certified") in (True, False), item.get("dataset_alias")


def test_profiles_do_not_carry_derivable_fields(tmp_path: Path):
    """默认维度指标可由 CSV 实时生成，留在画像里只会与元数据漂移。"""
    profiles = json.loads(
        (SKILL_ROOT / "data" / "dataset_profiles.json").read_text(encoding="utf-8")
    )
    for item in profiles["datasets"]:
        assert "default_dimensions" not in item
        assert "default_metrics" not in item
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/skills/test_local_fallback.py -k profiles -v
```

Expected: 三条全部 FAIL

- [ ] **Step 3: 改造画像文件**

运行以下一次性迁移脚本（跑完即可删除，不入库）：

```bash
cd /Users/mask/python3/opscli && source .venv/bin/activate && python3 - << 'PY'
import csv, json, pathlib

skill = pathlib.Path("opscli/skills/templates/ops-dataset-query")
# alias 以已安装目录的真实元数据为准（模板 data/ 是占位）
installed = pathlib.Path.home() / ".claude/skills/ops-dataset-query/data/datasets.csv"
rows = list(csv.DictReader(open(installed, encoding="utf-8-sig")))
alias_by_name = {
    (r.get("description") or r.get("dataset_name")): r["dataset_alias"] for r in rows
}

path = skill / "data/dataset_profiles.json"
payload = json.loads(path.read_text(encoding="utf-8"))
migrated = []
for item in payload["datasets"]:
    name = item.get("standard_name")
    alias = alias_by_name.get(name) or item.get("dataset_alias") or ""
    entry = {
        "dataset_alias": alias,
        "standard_name": name,
        # 无法从元数据推导的人工语义，只留这三类
        "hard_constraints": item.get("hard_constraints") or [],
        "clarify_when": item.get("clarify_when") or [],
        "avoid_when": item.get("avoid_when") or [],
        # 迁移一律置 false：未经人工复核的语义不作硬约束
        "certified": False,
        "reviewed_at": "",
    }
    if not alias:
        entry["stale_reason"] = "standard_name 在当前元数据中查不到对应 dataset_alias，需人工改绑或删除"
    migrated.append(entry)
payload["datasets"] = migrated
payload["schema_version"] = 2
path.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
print(f"已迁移 {len(migrated)} 份画像，其中 {sum(1 for e in migrated if not e['dataset_alias'])} 份查不到 alias")
PY
```

- [ ] **Step 4: 适配 local_fallback**

把 `local_fallback.py` 的 `_dataset_candidates()` 里按中文名查画像的两行：

```python
    profile_by_name = {
        str(item.get("standard_name", "")): item for item in profiles.get("datasets") or []
    }
```

改为按 alias 索引，并在组装候选时用 alias 反查：

```python
    # 按 alias 索引：按中文名索引会在数据集改名时静默腐烂，
    # 实测现有 15 份画像里有 5 份的 standard_name 已对不上后端
    profile_by_alias = {
        str(item.get("dataset_alias", "")): item
        for item in profiles.get("datasets") or []
        if item.get("dataset_alias")
    }
```

同一函数里把候选字典的三个画像字段改为 alias 查找 + 审核态分流：

```python
        alias = str(target.get("dataset_alias") or "")
        row = by_alias.get(alias, {})
        profile_entry = profile_by_alias.get(alias, {})
        certified = bool(profile_entry.get("certified"))
        scored.append(
            {
                "source": "intent_route",
                "intent_id": intent.get("intent_id"),
                "matched_terms": matched,
                "score": score,
                "confidence": confidence,
                "name_zh": intent.get("primary_dataset"),
                "execution_dataset": target.get("standard_name"),
                "dataset_alias": alias or None,
                "table_id": row.get("table_id"),
                "routing_status": profile.get("routing_status", "direct_intent"),
                # 已审核的业务约束才作硬约束；未审核的降级为提示，
                # 避免没人复核过的旧口径被当成权威规则执行
                "hard_constraints": profile_entry.get("hard_constraints") or [] if certified else [],
                "uncertified_hints_zh": [] if certified else (profile_entry.get("hard_constraints") or []),
                "avoid_when": profile_entry.get("avoid_when") or [],
                "clarify_when": (intent.get("clarify_when") or [])
                + (profile_entry.get("clarify_when") or []),
                # 默认维度指标改为实时取，不再从画像读
                "default_dimensions": _default_field_labels(resolved_dir, alias, "dimension"),
                "default_metrics": _default_field_labels(resolved_dir, alias, "metric"),
            }
        )
```

`_dataset_candidates()` 的签名相应改为接收 `resolved_dir`：`def _dataset_candidates(query, data_dir, profiles)` 已有 `data_dir`，把上面用到的 `resolved_dir` 改写为 `data_dir` 即可。并新增实时取字段的辅助函数：

```python
def _default_field_labels(data_dir: Path, dataset_alias: str, field_type: str, limit: int = 8) -> list:
    """实时从 CSV 取该数据集的代表性字段中文名。

    为什么不再写进画像：默认维度指标完全可由元数据派生，
    写进人工文件只会与后端漂移——实测 15 份画像已有 5 份对不上。
    """
    if not dataset_alias:
        return []
    labels = [
        str(row.get("verbose_name", ""))
        for row in core.load_csv_rows(Path(data_dir) / "dataset_fields.csv")
        if str(row.get("dataset_alias", "")) == dataset_alias
        and str(row.get("field_type", "")) == field_type
        and row.get("verbose_name")
    ]
    return list(dict.fromkeys(labels))[:limit]
```

- [ ] **Step 5: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_local_fallback.py -q
```

Expected: 全部 passed（含既有 5 条 xfail）

- [ ] **Step 6: 提交**

```bash
git add opscli/skills/templates/ops-dataset-query/data/dataset_profiles.json opscli/skills/templates/ops-dataset-query/scripts/local_fallback.py tests/skills/test_local_fallback.py
git commit -m "refactor(skills): 画像按 alias 索引并删除可派生字段"
```

---

### Task 7: 覆盖率巡检命令

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/local_fallback.py`
- Test: `tests/skills/test_local_fallback.py`

**Interfaces:**
- Consumes: Task 6 的画像新结构
- Produces: `python3 scripts/local_fallback.py --audit` 输出 `{total_datasets, profiled, certified, missing_profiles: [...], stale_profiles: [...]}`

- [ ] **Step 1: 写失败的测试**

```python
def test_audit_reports_coverage_gap(tmp_path: Path):
    """巡检必须报出未建画像与已腐烂的画像，让维护成本可见而不是靠人想起来。"""
    data_dir = tmp_path / "data"
    _write_ready_data_dir(data_dir)

    report = local_fallback.audit_profiles(data_dir=data_dir)
    assert report["total_datasets"] >= 1
    assert "missing_profiles" in report and "stale_profiles" in report
    assert isinstance(report["certified"], int)
```

- [ ] **Step 2: 运行确认失败**

```bash
python -m pytest tests/skills/test_local_fallback.py -k audit -v
```

Expected: FAIL，`AttributeError: module 'local_fallback' has no attribute 'audit_profiles'`

- [ ] **Step 3: 实现巡检**

在 `local_fallback.py` 新增：

```python
def audit_profiles(*, data_dir=None) -> dict:
    """巡检人工语义覆盖率：哪些数据集没有画像、哪些画像已对不上元数据。

    为什么需要：画像靠人工维护，没有巡检就只能等出错才发现。
    实测本仓库 60 个数据集只有 15 份画像、其中 5 份已腐烂。
    """
    resolved = Path(data_dir) if data_dir else core.discover_data_dir()
    if resolved is None or not resolved.is_dir():
        return {"status": "blocked", "next_action_zh": "本地数据目录不存在，先执行 opscli skills upgrade ops-dataset-query。"}
    datasets = core.load_csv_rows(resolved / "datasets.csv")
    aliases = {str(row.get("dataset_alias", "")) for row in datasets if row.get("dataset_alias")}
    profiles = _load_profiles(resolved).get("datasets") or []
    profiled = {str(item.get("dataset_alias", "")) for item in profiles if item.get("dataset_alias")}
    return {
        "status": "ok",
        "total_datasets": len(aliases),
        "profiled": len(profiled & aliases),
        "certified": sum(1 for item in profiles if item.get("certified")),
        "missing_profiles": sorted(aliases - profiled),
        "stale_profiles": sorted(profiled - aliases),
        "next_action_zh": "missing_profiles 中的数据集尚无人工语义，按需补充并置 certified=true；stale_profiles 已对不上元数据，应删除或改绑 alias。",
    }
```

在 `_parse_args()` 增加 `parser.add_argument("--audit", action="store_true", help="巡检画像覆盖率，不做查询")`，并在 `main()` 里于构建降级合同之前处理：

```python
    if args.audit:
        print(json.dumps(audit_profiles(data_dir=Path(args.data_dir) if args.data_dir else None), ensure_ascii=False))
        return 0
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_local_fallback.py -q
```

Expected: 全部 passed

- [ ] **Step 5: 真实数据跑一次巡检**

```bash
cd ~/.claude/skills/ops-dataset-query/scripts
cp /Users/mask/python3/opscli/opscli/skills/templates/ops-dataset-query/scripts/local_fallback.py .
python3 local_fallback.py --audit x | python3 -m json.tool
```

Expected: 输出 `total_datasets: 60` 量级、`missing_profiles` 列出未覆盖数据集

- [ ] **Step 6: 同步四目录、记录变更并提交**

```bash
cd /Users/mask/python3/opscli
for d in ~/.opscli/skills ~/.codex/skills ~/.claude/skills ~/.openclaw/skills; do
  [ -d "$d/ops-dataset-query" ] && cp opscli/skills/templates/ops-dataset-query/scripts/*.py "$d/ops-dataset-query/scripts/" && cp opscli/skills/templates/ops-dataset-query/data/dataset_profiles.json "$d/ops-dataset-query/data/"
done
git add -A opscli tests docs/change-log-pending.md
git commit -m "feat(skills): 新增画像覆盖率巡检命令"
```

---

## 不在本计划内（需各自单独成文）

以下两项都会改变系统边界，且各自应产出独立可用的软件，按 writing-plans 的范围检查应拆成单独计划：

**C. 意图表转自动 NL 卡片 + 领域词自动扩展**
依赖阶段二的三层度量先建立基线，否则无法证明删掉 `intent_rules.json` 的 8 个写死领域后路由没有变差。建议阶段一二落地并观察一轮后再启动。

**E. 60 个数据集按业务域切分 + 上层路由**
研究结论最硬的一条（Databricks Genie 硬上限 30 表、官方建议 ≤5），但涉及 Skill 拆分与 Agent 编排，改动面远超本计划。应在 C 之后单独立项。

---

## 待确认事项

1. **模糊匹配阈值 0.5** 取自 EDBT'26 论文，但本项目是中文字段名，最优阈值可能不同。Task 5 的三层评测建立基线后，应在 0.4/0.5/0.6 上各跑一次评测再定值。
2. **画像 `certified` 初值**：本计划把现有 15 份全部置 `false`（保守，未审核不作硬约束）。若认为现有画像内容可信，可改为全部置 `true` 再逐步复核——需要明确选择。
3. **ASIN 裸 token 策略**（现为"照样写筛选"）与本计划无关但仍未定，可一并处理。

---

## 阶段零（P0，最高优先，插在阶段一之前执行）：修复固定槽位过约束

> **本阶段是根因修复，取代原计划对零候实因的错误判断。**
> 原计划把「业务词零候选」归因于 `dataset_guidance._field_score()` 的 token 交集兜底，
> 执行 Task 2 时经控制者实测推翻：该处实际得分为 3 分，根本没走到。
> 真因经实测钉死在 `agent_query_planner._slot_is_covered()`：
> `slot_mode == "fixed"` 时要求数据集支持的槽位取值与用户请求**完全相等**。
> 「亚马逊搜索词绩效」支持 `grain={keyword, search_term}`、mode=fixed，
> 用户只说「搜索词」→ requested={search_term} → `supported-requested={keyword}` 非空 → 拒绝，
> 60 张卡片无一通过。对照实验：原文同时提「搜索词和关键词」→ 3 个正确数据集全部通过。
> 即**用户描述越具体，候选越少到零**，与「召回优先」原则直接冲突。
>
> 已在真实元数据上原型验证放开后的影响面（控制者实测）：
> 「搜索词的点击份额和购买份额」0→3（三个搜索词数据集全中）；
> 「SP关键词表现怎么样」1→3；「各平台广告投入和ACOS」20→20；
> 「查渠道和ASIN」60→60；「近30天各渠道销售额」60→60。
> 只影响被 grain 过约束的查询，其余零变化。
>
> 决策（用户已裁决）：采用「放开 + 强制披露」。放开后数据集粒度可能比用户要求更细，
> 该风险由合同强制披露兜住，不靠拒绝候选来规避。

### Task 8: 放开固定槽位并强制披露更细粒度

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/agent_query_planner.py`
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/query_plan.py`
- Modify: `opscli/skills/templates/ops-dataset-query/data/query_plan.schema.json`
- Test: `tests/skills/test_slot_coverage.py`（新建）

**Interfaces:**
- Consumes: 既有 `_expanded_values(name, values, rules)`、`profile["slots"]`、`profile["slot_modes"]`
- Produces:
  - `_slot_is_covered(profile, name, requested, rules) -> bool`（放开后语义）
  - `_extra_slot_terms(profile, slots, rules) -> dict[str, list[str]]` — 每个固定槽位多出的取值
  - 候选字典新增公开键 `grain_coverage: {"requested": [...], "extra": [...]}`
  - `model_view["grain_disclosure_zh"]: list[str]`

- [ ] **Step 1: 写失败的测试**

创建 `tests/skills/test_slot_coverage.py`：

```python
"""固定槽位覆盖判定的回归测试。

线上事故形态：用户说「搜索词的点击份额」返回零候选，而「搜索词和关键词的点击份额」
返回 3 个正确数据集——用户描述越具体候选越少。根因是固定槽位要求
数据集支持的取值与请求完全相等，覆盖更多粒度的数据集反而被拒。
"""

from __future__ import annotations

import sys
from pathlib import Path

import pytest

SCRIPTS_DIR = (
    Path(__file__).parents[2]
    / "opscli" / "skills" / "templates" / "ops-dataset-query" / "scripts"
)
if str(SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPTS_DIR))

import agent_query_planner as aqp  # noqa: E402

RULES = {
    "slots": {
        "grain": {
            "keyword": {"terms": ["关键词"], "description_patterns": ["关键词"]},
            "search_term": {"terms": ["搜索词"], "description_patterns": ["搜索词"]},
        }
    }
}


def _profile(supported: set, mode: str) -> dict:
    return {"slots": {"grain": supported}, "slot_modes": {"grain": mode}}


def test_dataset_covering_more_grains_is_accepted():
    """数据集支持 keyword+search_term，用户只要 search_term——应通过。

    这是零候选的直接成因：覆盖面更广的数据集本该可用，却因「不完全相等」被拒。
    """
    profile = _profile({"keyword", "search_term"}, "fixed")
    assert aqp._slot_is_covered(profile, "grain", {"search_term"}, RULES) is True


def test_dataset_missing_requested_grain_is_rejected():
    """数据集不支持用户要求的粒度时仍须拒绝——放开的是「多」，不是「少」。"""
    profile = _profile({"keyword"}, "fixed")
    assert aqp._slot_is_covered(profile, "grain", {"search_term"}, RULES) is False


def test_unsupported_slot_mode_still_rejected():
    """槽位标记为 unsupported 时不因放开而通过。"""
    profile = _profile({"search_term"}, "unsupported")
    assert aqp._slot_is_covered(profile, "grain", {"search_term"}, RULES) is False


def test_extra_slot_terms_reports_uncovered_surplus():
    """多出的粒度必须能被取出，供合同强制披露。"""
    profile = _profile({"keyword", "search_term"}, "fixed")
    extra = aqp._extra_slot_terms(profile, {"grain": {"search_term"}}, RULES)
    assert extra == {"grain": ["keyword"]}


def test_extra_slot_terms_empty_when_exact_match():
    """粒度正好相等时没有可披露内容，不应产生空噪声。"""
    profile = _profile({"search_term"}, "fixed")
    assert aqp._extra_slot_terms(profile, {"grain": {"search_term"}}, RULES) == {}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/mask/python3/opscli && source .venv/bin/activate
python -m pytest tests/skills/test_slot_coverage.py -v
```

Expected: `test_dataset_covering_more_grains_is_accepted` FAIL（返回 False）；两条 `_extra_slot_terms` 用例 FAIL（函数不存在）

- [ ] **Step 3: 放开判定并新增披露函数**

把 `agent_query_planner.py` 的 `_slot_is_covered` 整个函数替换为：

```python
def _slot_is_covered(profile: dict, name: str, requested: set[str], rules: dict) -> bool:
    """判断数据集画像是否覆盖请求的槽位取值。

    只要求数据集覆盖用户请求（requested ⊆ supported）。曾额外要求固定口径
    必须与请求完全相等，导致覆盖粒度更广的数据集被拒——实测「搜索词的点击份额」
    因此零候选，而「搜索词和关键词的点击份额」反而能命中三个正确数据集，
    形成「说得越具体候选越少」的反常行为。按召回优先原则放开，
    多出的粒度改由 _extra_slot_terms 交给合同强制披露。
    """
    raw_supported = profile["slots"][name]
    mode = profile["slot_modes"][name]
    if mode in {"unsupported", "unknown"} or not raw_supported:
        return False
    supported = _expanded_values(name, raw_supported, rules)
    requested = _expanded_values(name, requested, rules)
    return requested.issubset(supported)


def _extra_slot_terms(profile: dict, slots: dict, rules: dict) -> dict:
    """列出数据集比用户请求多覆盖的固定槽位取值，供强制披露。

    放开覆盖判定后，选中的数据集粒度可能比用户要的更细（如用户要搜索词级，
    数据集是关键词×搜索词级），直接汇总会与用户预期的口径不一致。
    风险不靠拒绝候选规避，而是如实告知——这是「放开 + 强制披露」的后半段。
    """
    extra: dict = {}
    for name, requested in (slots or {}).items():
        if name not in profile.get("slots", {}):
            continue
        if profile.get("slot_modes", {}).get(name) != "fixed":
            continue
        supported = _expanded_values(name, profile["slots"][name], rules)
        surplus = sorted(supported.difference(_expanded_values(name, requested, rules)))
        if surplus:
            extra[name] = surplus
    return extra
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_slot_coverage.py -v
```

Expected: 5 passed

- [ ] **Step 5: 把多出粒度带进候选字典**

在 `agent_query_planner.py` 的 `_score_profile()` 返回字典中，`"reasons": reasons,` 之后插入一行（`grain_coverage` 不带下划线前缀，因此会被 `_result` 保留为公开字段）：

```python
        "grain_coverage": _extra_slot_terms(profile, slots, rules),
```

- [ ] **Step 6: 在合同中强制披露**

在 `query_plan.py` 中，紧邻既有 `platform_scope_disclosures_zh` 的写入处（`model_view["platform_scope_disclosures_zh"] = platform_disclosures` 那一段之后）加入：

```python
    # 放开固定槽位后，选中数据集的粒度可能比用户要求更细，必须如实告知，
    # 否则用户会把「关键词×搜索词」级明细当成「搜索词」级汇总
    grain_extra = (selection.get("selected_candidate") or {}).get("grain_coverage") or {}
    if grain_extra:
        model_view["grain_disclosure_zh"] = [
            f"所选数据集的{name}粒度比请求更细，额外覆盖：{'、'.join(terms)}；"
            "结论中必须说明这一口径差异，不得把更细粒度的明细当成请求粒度的汇总。"
            for name, terms in grain_extra.items()
        ]
        answer_contract["required_disclosures_zh"].extend(model_view["grain_disclosure_zh"])
```

若该处 `selection` 变量名或选中候选的取法与上述不符，按当前文件实际变量名调整取值路径，但必须满足：`grain_coverage` 非空时 `model_view["grain_disclosure_zh"]` 与 `answer_contract["required_disclosures_zh"]` 都要写入。

- [ ] **Step 7: 登记 schema 新字段**

在 `data/query_plan.schema.json` 的 `properties.model_view.properties` 中加入：

```json
        "grain_disclosure_zh": {
          "description": "所选数据集粒度比请求更细时的强制披露",
          "$ref": "#/$defs/string_array"
        }
```

- [ ] **Step 8: 实测验证零候选被修复且不误伤**

```bash
for d in ~/.claude/skills; do cp opscli/skills/templates/ops-dataset-query/scripts/{agent_query_planner.py,query_plan.py} "$d/ops-dataset-query/scripts/"; cp opscli/skills/templates/ops-dataset-query/data/query_plan.schema.json "$d/ops-dataset-query/data/"; rm -rf "$d/ops-dataset-query/scripts/__pycache__"; done
cd ~/.claude/skills/ops-dataset-query/scripts
for q in "看一下搜索词的点击份额和购买份额" "SP关键词表现怎么样" "查渠道和ASIN" "近30天各渠道销售额"; do
  /bin/zsh -lc "cd $PWD && python3 query_plan.py '$q' --no-auto-enum 2>/dev/null" | python3 -c "
import json,sys
d=json.load(sys.stdin); mv=d.get('model_view') or {}
print(f\"  status={d.get('status'):16} 选中={mv.get('dataset_name_zh') or '（未定）'} 披露={(mv.get('grain_disclosure_zh') or ['无'])[0][:34]}\")"
done
```

Expected: 第一条不再零候选（应选中或列出搜索词类数据集），且带 `grain_disclosure_zh`；后两条行为不变

- [ ] **Step 9: 全量回归**

```bash
cd /Users/mask/python3/opscli
python -m pytest tests/skills/test_slot_coverage.py tests/skills/test_dataset_query_planner.py tests/skills/test_local_fallback.py tests/skills/test_component_filter_resolution.py tests/query -q
```

Expected: 全部 passed（既有 xfail 保持 xfail）

- [ ] **Step 10: 提交**

```bash
git add opscli/skills/templates/ops-dataset-query/scripts/{agent_query_planner.py,query_plan.py} opscli/skills/templates/ops-dataset-query/data/query_plan.schema.json tests/skills/test_slot_coverage.py
git commit -m "fix(planner): 放开固定槽位过约束并强制披露更细粒度"
```

---

### Task 9: 修正模糊匹配设计为分段 + 双向包含

> **本任务修的是 Task 1/2 的设计缺陷，不是新功能。**
> Task 2 已提交（2bebd27）但经控制者实测为**空操作**：`best_fuzzy_score` 拿【整句】与字段名比二元组相似度，
> 长查询会把相似度稀释到阈值以下——实测「看一下搜索词的点击份额和购买份额」
> 对完全同名的字段「点击份额」相似度仅 0.214，远低于 0.5 阈值，模糊分永不触发。
>
> 零候选已由 Task 8 从根因修复，本任务的目标改为**字段召回**：
> 现有 `_field_score` 只检查「字段中文名 in 查询」的**单向**包含，
> 当字段名比用户说法长时（字段「ASIN点击份额」vs 用户「点击份额」）正向不成立，
> 只能靠 token 交集拿 3 分，几乎排不进候选。
>
> 控制者已原型验证修正设计（分段 + 双向包含 + 分段二元组相似度）：
> 「点击份额」→ 字段「ASIN点击份额」0.90、「ASIN购买份额」0.90、「总库存」0.00；
> 「各渠道销售额」→「销售额」0.90、「广告费」0.00。判别力正常。
> 由于 `_field_score` 把模糊分压在 50 以内，精确命中（80/90/100 档）仍然优先。

**Files:**
- Modify: `opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py`
- Modify: `tests/skills/test_schema_completion.py`

**Interfaces:**
- Consumes: 既有 `bigram_similarity`、`FUZZY_MATCH_THRESHOLD`、`MIN_FUZZY_LENGTH`
- Produces: `best_fuzzy_score(query_text, labels)` 语义改为「查询分段后与标签逐段比对取最高分」，签名不变（Task 2 的调用点无需改动）

- [ ] **Step 1: 写失败的测试**

在 `tests/skills/test_schema_completion.py` 追加：

```python
@BOTH_VERSIONS
def test_long_query_still_matches_short_label(module):
    """整句比对会被长度稀释：完全同名的字段也只有 0.214，模糊分永不触发。

    这是 Task 2 交付即空操作的直接成因，必须按分段比对修正。
    """
    query = "看一下搜索词的点击份额和购买份额"
    assert module.bigram_similarity(query, "点击份额") < module.FUZZY_MATCH_THRESHOLD
    assert module.best_fuzzy_score(query, ["点击份额"]) >= module.FUZZY_MATCH_THRESHOLD


@BOTH_VERSIONS
def test_field_label_longer_than_user_phrase_matches(module):
    """字段名比用户说法长时也要命中——现有单向包含在这里失效。

    实测字段「ASIN点击份额」对用户词「点击份额」仅靠 token 交集拿 3 分。
    """
    query = "看一下搜索词的点击份额和购买份额"
    assert module.best_fuzzy_score(query, ["ASIN点击份额"]) >= module.FUZZY_MATCH_THRESHOLD


@BOTH_VERSIONS
def test_unrelated_label_stays_zero_after_segmentation(module):
    """分段不能把无关字段拉进来，否则召回优先变成没有判别力。"""
    query = "看一下搜索词的点击份额和购买份额"
    assert module.best_fuzzy_score(query, ["总库存"]) == 0.0
    assert module.best_fuzzy_score("查近30天各渠道销售额", ["广告费"]) == 0.0
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /Users/mask/python3/opscli && source .venv/bin/activate
python -m pytest tests/skills/test_schema_completion.py -k "long_query or longer_than or after_segmentation" -v
```

Expected: 前两条 FAIL（返回 0.0，低于阈值）

- [ ] **Step 3: 改写 best_fuzzy_score**

把 `schema_completion.py` 的 `best_fuzzy_score` 整个函数替换为：

```python
# 查询分段用的分隔符：中文助词、连词与标点。分段是为了不让长句稀释相似度
_SEGMENT_SPLIT_RE = re.compile(r"[的和与及或，,。.；;、\s]+")


def _query_segments(query_text: str) -> list:
    """把查询切成候选业务词片段，过滤掉短到无判别力的碎片。"""
    return [
        segment
        for segment in _SEGMENT_SPLIT_RE.split(_normalize(query_text))
        if len(segment) >= MIN_FUZZY_LENGTH
    ]


def best_fuzzy_score(query_text: str, labels: list) -> float:
    """取查询与一组字段标签的最高匹配度，低于阈值归零。

    为什么按片段而不是整句比对：整句会被长度稀释——实测
    「看一下搜索词的点击份额和购买份额」对完全同名的字段「点击份额」
    相似度只有 0.214，远低于 0.5 阈值，模糊分因此永不触发。

    为什么要双向包含：字段中文名常比用户说法长（字段「ASIN点击份额」
    对用户词「点击份额」），规划器既有的单向判断「字段名 in 查询」在这里
    不成立，只能靠 token 交集拿个位数分。双向包含按 0.9 计，
    仍低于 _field_score 的精确命中档位，不会挤掉准确字段。
    """
    segments = _query_segments(query_text)
    if not segments:
        return 0.0
    best = 0.0
    for label in labels:
        normalized_label = _normalize(label)
        if len(normalized_label) < MIN_FUZZY_LENGTH:
            continue
        for segment in segments:
            if segment in normalized_label or normalized_label in segment:
                best = max(best, 0.9)
                continue
            best = max(best, bigram_similarity(segment, normalized_label))
    return best if best >= FUZZY_MATCH_THRESHOLD else 0.0
```

同时在文件顶部导入区补 `import re`（放在 `import unicodedata` 之前，保持字母序）。

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/skills/test_schema_completion.py -v
```

Expected: 全部 passed（含 Task 1/2 既有用例）

- [ ] **Step 5: 验证字段打分确实被拉起**

```bash
python3 - << 'PY'
import sys; sys.path.insert(0, "opscli/skills/templates/ops-dataset-query/scripts")
import dataset_guidance as dg
q = dg._normalize("看一下搜索词的点击份额和购买份额")
for label, expect in (("ASIN点击份额", "应显著高于 3"), ("总库存", "应为 0")):
    f = {"field_name": "x", "verbose_name": label, "field_type": "metric", "dataset_alias": "d"}
    print(f"{label}: {dg._field_score(f, q, dg._tokens(q))}  ({expect})")
PY
```

Expected: 「ASIN点击份额」得分显著高于修正前的 3（应为 40 上下）；「总库存」仍为 0

- [ ] **Step 6: 全量回归**

```bash
python -m pytest tests/skills/test_schema_completion.py tests/skills/test_dataset_query_planner.py tests/skills/test_slot_coverage.py tests/skills/test_local_fallback.py tests/skills/test_component_filter_resolution.py tests/query -q
```

Expected: 全部 passed，既有 xfail 保持 xfail

- [ ] **Step 7: 提交**

```bash
git add opscli/skills/templates/ops-dataset-query/scripts/schema_completion.py tests/skills/test_schema_completion.py
git commit -m "fix(planner): 模糊匹配改为分段加双向包含，修正整句比对永不触发"
```
