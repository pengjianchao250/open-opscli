# 用户级元数据缓存基础设施 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 opscli 提供一套按用户隔离、CLI 与 MCP 共用、带 1 小时 TTL 与并发文件锁的全量元数据缓存，使取数规划器（后续计划）迁入内核后不必每次向后端拉全量元数据。

**Architecture:** 复刻 `opscli/mcp/credential_cache.py` 的"模块级池 + 两层缓存（进程内内存 L1 + 磁盘 JSON L2）"范式。缓存对象身份无关，只接收 `(base_dir, user_email, fetch_fn)`，身份由 CLI/MCP 调用方注入（守铁律2：query 不 import mcp）。磁盘缓存文件按 `sha256(email)[:16]` 命名，落在凭证隔离目录下，天然复用现有多用户隔离。

**Tech Stack:** Python 3.10+，标准库（`hashlib`/`json`/`tempfile`/`threading`/`fcntl`/`contextlib`/`dataclasses`），测试用 `pytest` + `respx`（HTTP mock）+ `tmp_path`（临时目录）。

## Global Constraints

- Python >= 3.10；可用 `X | Y` 联合类型（copy 自 CLAUDE.md 代码规范）。
- 所有代码注释与 docstring 使用中文，关键业务逻辑逐段注释（铁律17）。
- 测试禁止真实网络与系统 Keychain：网络用 `respx` mock，路径用 `tmp_path`，`base_dir=tmp_path` 传入（铁律8）。
- 缓存 TTL 默认 **3600 秒（1 小时）**；登录时强制失效（已确认决策）。
- 后端拉取失败时返回过期缓存并置 `stale=True`，不硬失败（已确认决策）。
- 磁盘缓存文件按 **email 哈希** 命名（已确认决策）。
- 依赖方向：`opscli.query.*` 禁止 import `opscli.mcp.*`；`opscli.config` 只依赖标准库（铁律2）。
- HTTP 客户端统一 `httpx`；元数据类请求超时 20s（沿用 `QueryClient` 现状）。
- 每次代码修改后向 `docs/change-log-pending.md` 追加变更记录（铁律18）。

**范围边界（本计划不做）：** 后端 `data-metrics` 的 `include_all_fields` 参数与字段白名单改造（外部前置，见 `docs/plans/取数规划器内核化与用户级元数据缓存实施计划.md` Phase 0）；规划器五文件迁入内核（后续独立计划 Phase 4）。本计划交付的缓存在后端 `include_all_fields` 上线前，`metadata_all()` 仍可工作（后端未识别该参数时返回裸 datasets，缓存逻辑不受影响），但字段全量需待后端就绪。

---

## File Structure

| 文件 | 职责 | 本计划动作 |
| --- | --- | --- |
| `opscli/shared/file_lock.py` | 跨进程独占文件锁 context manager（从 token_manager 抽取的可复用零件） | 新建 |
| `opscli/query/services/metadata_cache.py` | 用户级元数据缓存：`MetadataCache` 类 + `MetadataCacheResult` + 模块级池 | 新建 |
| `opscli/query/transport/client.py` | `QueryClient.fetch_query_metadata` 增加 `include_all_fields` 参数 | 改 126-165 |
| `opscli/query/services/manager.py` | `QueryManager` 新增 `metadata_all()` 经缓存返回全量 payload | 改（加方法） |
| `opscli/auth/cli.py` | `login`/`logout` 命令成功后失效元数据缓存 | 改 70-115 |
| `opscli/mcp/tools/auth.py` | MCP 登录/登出工具在 `invalidate_credential_cache` 旁失效元数据缓存 | 改（多点） |
| `tests/shared/test_file_lock.py` | file_lock 单测 | 新建 |
| `tests/query/test_metadata_cache.py` | 缓存单测 | 新建 |
| `tests/query/test_metadata_all.py` | metadata_all 集成单测 | 新建 |
| `tests/auth/test_login_cache_invalidation.py` | 登录失效挂钩单测 | 新建 |

---

## Task 1: 抽取共享文件锁 file_lock

**Files:**
- Create: `opscli/shared/file_lock.py`
- Test: `tests/shared/test_file_lock.py`

**Interfaces:**
- Consumes: 无（纯标准库）。
- Produces: `file_lock(path: Path) -> ContextManager[None]`——获取基于 `fcntl.flock` 的独占锁；无 `fcntl`（Windows）降级为空操作。Task 4/5 使用。

- [ ] **Step 1: 写失败测试**

创建 `tests/shared/test_file_lock.py`：

```python
"""file_lock 文件锁单测。"""
from pathlib import Path

from opscli.shared.file_lock import file_lock


def test_file_lock_basic_acquire_and_release(tmp_path: Path):
    """能进入/退出上下文，并在锁目录不存在时自动创建父目录。"""
    lock_path = tmp_path / "sub" / ".lock_x"
    with file_lock(lock_path):
        # 锁上下文内可正常执行业务
        (tmp_path / "sub" / "marker").write_text("ok", encoding="utf-8")
    assert (tmp_path / "sub" / "marker").read_text(encoding="utf-8") == "ok"


def test_file_lock_reentrant_sequential(tmp_path: Path):
    """同一路径先后两次获取锁应都成功（释放后可再获取）。"""
    lock_path = tmp_path / ".lock_y"
    with file_lock(lock_path):
        pass
    with file_lock(lock_path):
        pass
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/shared/test_file_lock.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'opscli.shared.file_lock'`

- [ ] **Step 3: 写最小实现**

创建 `opscli/shared/file_lock.py`：

```python
"""跨进程独占文件锁工具。

从 auth/core/token_manager.py 内联的 flock 逻辑抽取为可复用 context manager，
供元数据缓存等需要跨进程串行化刷新的场景使用。

Windows 无 fcntl，降级为空操作（仅依赖调用方的进程内线程锁）。
"""
from __future__ import annotations

from contextlib import contextmanager
from pathlib import Path
from typing import Iterator

# fcntl 仅 POSIX 可用；Windows 上导入失败则跳过跨进程锁
try:
    import fcntl

    _FCNTL_AVAILABLE = True
except ImportError:  # pragma: no cover - Windows 分支
    _FCNTL_AVAILABLE = False


@contextmanager
def file_lock(path: Path) -> Iterator[None]:
    """获取基于 fcntl.flock 的独占文件锁。

    Args:
        path: 锁文件路径；父目录不存在时自动创建。

    行为：
        - POSIX：以独占方式 flock，退出时解锁。
        - Windows（无 fcntl）：直接进入/退出，不加跨进程锁。
    """
    if not _FCNTL_AVAILABLE:
        # Windows 降级：无跨进程锁，直接放行
        yield
        return

    path.parent.mkdir(parents=True, exist_ok=True)
    # 以写模式打开锁文件；flock 作用于该文件描述符
    with open(path, "w") as lock_file:
        try:
            fcntl.flock(lock_file, fcntl.LOCK_EX)  # 独占锁，阻塞直至获取
            yield
        finally:
            fcntl.flock(lock_file, fcntl.LOCK_UN)  # 显式解锁（关闭 fd 也会释放）
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/shared/test_file_lock.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 记录变更并提交**

向 `docs/change-log-pending.md` 追加一条（参照铁律18 模板：原因=抽取可复用文件锁供缓存使用；改动点=新增 file_lock.py；验证=单测通过；影响=新增工具无副作用；回滚=删除文件）。

```bash
git add opscli/shared/file_lock.py tests/shared/test_file_lock.py docs/change-log-pending.md
git commit -m "feat(shared): 抽取可复用 file_lock 跨进程文件锁"
```

---

## Task 2: 缓存信封与新鲜度判定（纯函数）

**Files:**
- Create: `opscli/query/services/metadata_cache.py`
- Test: `tests/query/test_metadata_cache.py`

**Interfaces:**
- Consumes: `opscli.config.CONFIG_DIR`。
- Produces:
  - `MetadataCacheResult` dataclass：`payload: dict`、`stale: bool`、`fetched_at: datetime`、`from_cache: bool`。
  - `MetadataCache(base_dir: Path | None = None, ttl_seconds: int = 3600)`，内部方法 `_email_hash(email) -> str`、`_envelope(email, payload) -> dict`、`_is_fresh(env, email) -> bool`。Task 3-5 依赖。

- [ ] **Step 1: 写失败测试**

创建 `tests/query/test_metadata_cache.py`：

```python
"""用户级元数据缓存单测。"""
from datetime import datetime, timedelta, timezone
from pathlib import Path

from opscli.query.services.metadata_cache import MetadataCache


def _payload():
    return {"datasets": [{"dataset_alias": "ds_a"}], "fields": [], "select_columns": []}


def test_envelope_and_fresh(tmp_path: Path):
    """新建信封应被判定为新鲜，且 email 一致。"""
    cache = MetadataCache(base_dir=tmp_path, ttl_seconds=3600)
    env = cache._envelope("u@x.com", _payload())
    assert env["user_email"] == "u@x.com"
    assert env["cache_version"] == 1
    assert env["ttl_seconds"] == 3600
    assert cache._is_fresh(env, "u@x.com") is True


def test_stale_when_email_mismatch(tmp_path: Path):
    """信封 email 与当前用户不符时视为不新鲜（换账号失效）。"""
    cache = MetadataCache(base_dir=tmp_path)
    env = cache._envelope("old@x.com", _payload())
    assert cache._is_fresh(env, "new@x.com") is False


def test_stale_when_expired(tmp_path: Path):
    """超过 TTL 视为不新鲜。"""
    cache = MetadataCache(base_dir=tmp_path, ttl_seconds=60)
    env = cache._envelope("u@x.com", _payload())
    # 手动把抓取时间调到 2 分钟前
    env["fetched_at"] = (datetime.now(timezone.utc) - timedelta(minutes=2)).isoformat()
    assert cache._is_fresh(env, "u@x.com") is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'opscli.query.services.metadata_cache'`

- [ ] **Step 3: 写最小实现**

创建 `opscli/query/services/metadata_cache.py`：

```python
"""用户级元数据缓存。

为 CLI 与 MCP 提供按用户隔离、带 TTL 与并发文件锁的全量元数据缓存，
避免每次取数都向后端拉全量 query-metadata。

两层缓存：
    L1 进程内内存（服务 MCP 长驻进程高频读）
    L2 磁盘 JSON（服务 CLI 短生命进程跨调用复用）

身份无关：只接收 (base_dir, user_email, fetch_fn)，身份由调用方注入，
避免 query 反向依赖 mcp（铁律2）。
"""
from __future__ import annotations

import hashlib
import threading
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Callable

from opscli.config import CONFIG_DIR

_CACHE_VERSION = 1
_DEFAULT_TTL_SECONDS = 3600  # 默认 1 小时，可经调用方覆盖


@dataclass
class MetadataCacheResult:
    """缓存读取结果。

    Attributes:
        payload: 全量元数据 {"datasets": [...], "fields": [...], "select_columns": [...]}
        stale: True 表示后端拉取失败、回退到过期缓存
        fetched_at: 该 payload 的抓取时间（UTC）
        from_cache: True=命中缓存(L1/L2)，False=本次新抓取
    """

    payload: dict[str, Any]
    stale: bool
    fetched_at: datetime
    from_cache: bool


class MetadataCache:
    """按 base_dir 隔离的元数据缓存实例。线程安全。"""

    def __init__(self, base_dir: Path | None = None, ttl_seconds: int = _DEFAULT_TTL_SECONDS):
        # base_dir 为 None 时用默认 CONFIG_DIR（CLI/stdio 共享）；
        # MCP 多用户场景传入隔离目录。
        self._base = Path(base_dir) if base_dir else CONFIG_DIR
        self._dir = self._base / "metadata"
        self._ttl = ttl_seconds
        self._mem: dict[str, dict[str, Any]] = {}  # email_hash -> 信封
        self._lock = threading.Lock()

    @staticmethod
    def _email_hash(email: str) -> str:
        """按 email 生成 16 位十六进制缓存键（同机多账号隔离）。"""
        return hashlib.sha256(email.encode("utf-8")).hexdigest()[:16]

    def _envelope(self, email: str, payload: dict[str, Any]) -> dict[str, Any]:
        """构造磁盘/内存信封。"""
        return {
            "cache_version": _CACHE_VERSION,
            "user_email": email,
            "metadata_version": payload.get("metadata_version", ""),
            "fetched_at": datetime.now(timezone.utc).isoformat(),
            "ttl_seconds": self._ttl,
            "payload": payload,
        }

    def _is_fresh(self, env: dict[str, Any], email: str) -> bool:
        """命中判定：email 一致且未超 TTL。"""
        # 换账号即失效（对齐 CredentialStore email 变更清 tokens 的约定）
        if env.get("user_email") != email:
            return False
        try:
            fetched = datetime.fromisoformat(env["fetched_at"])
            if fetched.tzinfo is None:
                fetched = fetched.replace(tzinfo=timezone.utc)
        except (KeyError, ValueError):
            return False
        age = (datetime.now(timezone.utc) - fetched).total_seconds()
        return age < env.get("ttl_seconds", self._ttl)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 记录变更并提交**

追加 change-log（原因=缓存信封与新鲜度判定；验证=3 单测通过；回滚=删除文件）。

```bash
git add opscli/query/services/metadata_cache.py tests/query/test_metadata_cache.py docs/change-log-pending.md
git commit -m "feat(query): 元数据缓存信封与新鲜度判定"
```

---

## Task 3: 磁盘层读取与原子写

**Files:**
- Modify: `opscli/query/services/metadata_cache.py`
- Test: `tests/query/test_metadata_cache.py`

**Interfaces:**
- Consumes: Task 2 的 `MetadataCache`、`_email_hash`。
- Produces: `MetadataCache._cache_file(h) -> Path`、`_lock_file(h) -> Path`、`_read_disk(h) -> dict | None`、`_write_disk(h, env) -> None`（原子写，损坏文件读取返回 None）。Task 4 依赖。

- [ ] **Step 1: 写失败测试**

向 `tests/query/test_metadata_cache.py` 追加：

```python
def test_disk_round_trip(tmp_path: Path):
    """写入后能读回相同信封。"""
    cache = MetadataCache(base_dir=tmp_path)
    env = cache._envelope("u@x.com", _payload())
    h = cache._email_hash("u@x.com")
    cache._write_disk(h, env)
    assert cache._cache_file(h).exists()
    loaded = cache._read_disk(h)
    assert loaded["user_email"] == "u@x.com"
    assert loaded["payload"]["datasets"][0]["dataset_alias"] == "ds_a"


def test_disk_missing_returns_none(tmp_path: Path):
    """无缓存文件时返回 None。"""
    cache = MetadataCache(base_dir=tmp_path)
    assert cache._read_disk(cache._email_hash("nobody@x.com")) is None


def test_disk_corrupt_returns_none(tmp_path: Path):
    """损坏 JSON 返回 None 而非抛错。"""
    cache = MetadataCache(base_dir=tmp_path)
    h = cache._email_hash("u@x.com")
    cache._dir.mkdir(parents=True, exist_ok=True)
    cache._cache_file(h).write_text("{not json", encoding="utf-8")
    assert cache._read_disk(h) is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_cache.py -k disk -v`
Expected: FAIL — `AttributeError: 'MetadataCache' object has no attribute '_write_disk'`

- [ ] **Step 3: 写最小实现**

在 `metadata_cache.py` 的 `MetadataCache` 类中，`_is_fresh` 方法后追加：

```python
    def _cache_file(self, h: str) -> Path:
        """按 email 哈希定位缓存文件。"""
        return self._dir / f"{h}.json"

    def _lock_file(self, h: str) -> Path:
        """按 email 哈希定位锁文件（与缓存文件同目录）。"""
        return self._dir / f".lock_{h}"

    def _read_disk(self, h: str) -> dict[str, Any] | None:
        """读取磁盘信封；文件缺失或损坏返回 None。"""
        import json

        f = self._cache_file(h)
        if not f.exists():
            return None
        try:
            return json.loads(f.read_text(encoding="utf-8"))
        except (ValueError, OSError):
            # 损坏或读失败：视为无缓存，交由上层重新拉取
            return None

    def _write_disk(self, h: str, env: dict[str, Any]) -> None:
        """原子写信封：临时文件 + os.replace（照搬 updater 原子替换约定）。"""
        import json
        import os
        import tempfile

        self._dir.mkdir(parents=True, exist_ok=True)
        fd, tmp = tempfile.mkstemp(dir=self._dir, suffix=".tmp")
        try:
            with os.fdopen(fd, "w", encoding="utf-8") as fh:
                json.dump(env, fh, ensure_ascii=False)
            os.replace(tmp, self._cache_file(h))  # 同目录 rename，原子
        except BaseException:
            # 失败时清理临时文件，避免残留
            if os.path.exists(tmp):
                os.unlink(tmp)
            raise
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/query/services/metadata_cache.py tests/query/test_metadata_cache.py docs/change-log-pending.md
git commit -m "feat(query): 元数据缓存磁盘层原子读写"
```

---

## Task 4: get() 主流程（L1/L2/拉取 + 原子落盘）

**Files:**
- Modify: `opscli/query/services/metadata_cache.py`
- Test: `tests/query/test_metadata_cache.py`

**Interfaces:**
- Consumes: Task 2/3 的信封、新鲜度、磁盘层。
- Produces: `MetadataCache.get(user_email: str, fetch_fn: Callable[[], dict]) -> MetadataCacheResult`。Task 5/6 扩展其分支；Task 8 调用。

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_get_miss_then_hit(tmp_path: Path):
    """首次 miss 触发一次 fetch_fn 并落盘；二次命中不再 fetch。"""
    cache = MetadataCache(base_dir=tmp_path)
    calls = {"n": 0}

    def fetch():
        calls["n"] += 1
        return _payload()

    r1 = cache.get("u@x.com", fetch)
    assert r1.from_cache is False and r1.stale is False
    assert calls["n"] == 1

    r2 = cache.get("u@x.com", fetch)
    assert r2.from_cache is True
    assert calls["n"] == 1  # 未再拉取


def test_get_l2_hit_new_instance(tmp_path: Path):
    """新进程（新实例、空 L1）能命中已落盘的 L2 缓存。"""
    MetadataCache(base_dir=tmp_path).get("u@x.com", _payload)
    calls = {"n": 0}

    def fetch():
        calls["n"] += 1
        return _payload()

    r = MetadataCache(base_dir=tmp_path).get("u@x.com", fetch)
    assert r.from_cache is True
    assert calls["n"] == 0  # 直接读磁盘，未拉取
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_cache.py -k get -v`
Expected: FAIL — `AttributeError: 'MetadataCache' object has no attribute 'get'`

- [ ] **Step 3: 写最小实现**

在类中追加（引入 `file_lock`）。先在文件顶部 import 区加：

```python
from opscli.shared.file_lock import file_lock
```

再在 `_write_disk` 之后追加：

```python
    def _result(self, env: dict[str, Any], *, stale: bool, from_cache: bool) -> MetadataCacheResult:
        """把信封投影为对外结果对象。"""
        fetched = datetime.fromisoformat(env["fetched_at"])
        if fetched.tzinfo is None:
            fetched = fetched.replace(tzinfo=timezone.utc)
        return MetadataCacheResult(
            payload=env["payload"], stale=stale, fetched_at=fetched, from_cache=from_cache
        )

    def get(self, user_email: str, fetch_fn: Callable[[], dict[str, Any]]) -> MetadataCacheResult:
        """读取用户全量元数据，未命中/过期时经 fetch_fn 拉取并缓存。

        Args:
            user_email: 当前用户邮箱（缓存隔离维度）。
            fetch_fn: 无参回调，返回后端全量 payload。

        Returns:
            MetadataCacheResult。
        """
        h = self._email_hash(user_email)

        # L1 内存
        with self._lock:
            env = self._mem.get(h)
        if env and self._is_fresh(env, user_email):
            return self._result(env, stale=False, from_cache=True)

        # L2 磁盘
        env = self._read_disk(h)
        if env and self._is_fresh(env, user_email):
            with self._lock:
                self._mem[h] = env
            return self._result(env, stale=False, from_cache=True)

        # miss/过期 → 加跨进程锁刷新
        with file_lock(self._lock_file(h)):
            # 双重检查：可能有别的进程刚刷完（Task 5 强化并发语义）
            env2 = self._read_disk(h)
            if env2 and self._is_fresh(env2, user_email):
                with self._lock:
                    self._mem[h] = env2
                return self._result(env2, stale=False, from_cache=True)

            payload = fetch_fn()
            new_env = self._envelope(user_email, payload)
            self._write_disk(h, new_env)
            with self._lock:
                self._mem[h] = new_env
            return self._result(new_env, stale=False, from_cache=False)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: PASS（8 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/query/services/metadata_cache.py tests/query/test_metadata_cache.py docs/change-log-pending.md
git commit -m "feat(query): 元数据缓存 get 主流程与两层命中"
```

---

## Task 5: 并发防惊群（file_lock + 双重检查）

**Files:**
- Test: `tests/query/test_metadata_cache.py`（本任务验证 Task 4 的并发正确性；如测试暴露缺陷再修 `metadata_cache.py`）

**Interfaces:**
- Consumes: Task 4 的 `get`、Task 1 的 `file_lock`。
- Produces: 无新接口；确立"冷/过期缓存下 N 个并发调用只触发一次 `fetch_fn`"的行为契约。

- [ ] **Step 1: 写失败测试**

追加（多线程模拟并发；进程内 `file_lock` 由同一 fd 语义 + 双重检查保证只拉一次。注：进程内并发的最终防线是双重检查读盘，Task 4 已实现）：

```python
import threading
import time


def test_get_concurrent_single_fetch(tmp_path: Path):
    """10 个线程并发首取，只应触发一次 fetch_fn。"""
    cache = MetadataCache(base_dir=tmp_path)
    calls = {"n": 0}
    lock = threading.Lock()

    def fetch():
        with lock:
            calls["n"] += 1
        time.sleep(0.05)  # 放大竞态窗口
        return _payload()

    results = []

    def worker():
        results.append(cache.get("u@x.com", fetch))

    threads = [threading.Thread(target=worker) for _ in range(10)]
    for t in threads:
        t.start()
    for t in threads:
        t.join()

    assert len(results) == 10
    assert calls["n"] == 1  # 只拉一次，其余命中缓存
```

> 说明：Task 4 的 `get` 在跨进程锁内做了"重读磁盘 + 双重检查"。进程内并发时，第一个线程写盘后释放锁，后续线程进入锁再读盘即命中。若本测试失败（`calls["n"] > 1`），需在 `get` 的跨进程锁**外再包一层按 email_hash 的进程内 `threading.Lock`**（复用 `token_manager` 双层锁模式）再复测。

- [ ] **Step 2: 运行测试确认结果**

Run: `pytest tests/query/test_metadata_cache.py::test_get_concurrent_single_fetch -v`
Expected: PASS。若 FAIL（fetch 被调用多次），执行 Step 3 修复。

- [ ] **Step 3:（仅当 Step 2 失败）加进程内二级锁**

在 `MetadataCache.__init__` 增加按 hash 的线程锁字典：

```python
        self._refresh_locks: dict[str, threading.Lock] = {}
        self._refresh_locks_guard = threading.Lock()
```

在类中新增取锁辅助：

```python
    def _refresh_lock(self, h: str) -> threading.Lock:
        """获取/创建某用户的进程内刷新锁（防同进程多线程重复拉取）。"""
        with self._refresh_locks_guard:
            if h not in self._refresh_locks:
                self._refresh_locks[h] = threading.Lock()
            return self._refresh_locks[h]
```

把 `get` 中 `with file_lock(self._lock_file(h)):` 改为双层：

```python
        with self._refresh_lock(h), file_lock(self._lock_file(h)):
```

- [ ] **Step 4: 运行全部缓存测试确认通过**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add tests/query/test_metadata_cache.py opscli/query/services/metadata_cache.py docs/change-log-pending.md
git commit -m "test(query): 元数据缓存并发防惊群（双重检查/双层锁）"
```

---

## Task 6: 后端失败兜底（stale）与失效 + 模块级池

**Files:**
- Modify: `opscli/query/services/metadata_cache.py`
- Test: `tests/query/test_metadata_cache.py`

**Interfaces:**
- Consumes: Task 4 的 `get`。
- Produces:
  - `get` 在 `fetch_fn` 抛错且存在过期缓存时返回 `stale=True`；无任何缓存则原样抛错。
  - `MetadataCache.invalidate(user_email: str | None = None) -> None`（None=清全部）。
  - 模块级 `get_metadata_cache(base_dir=None, ttl_seconds=3600) -> MetadataCache`、`invalidate_metadata_cache(base_dir=None, user_email=None) -> None`。Task 8/9 调用。

- [ ] **Step 1: 写失败测试**

追加：

```python
import pytest


def test_get_stale_on_fetch_error(tmp_path: Path):
    """已有过期缓存时，后端拉取失败返回过期数据并标 stale。"""
    cache = MetadataCache(base_dir=tmp_path, ttl_seconds=60)
    cache.get("u@x.com", _payload)  # 先建缓存
    # 手动把磁盘缓存改成 2 分钟前（过期）
    h = cache._email_hash("u@x.com")
    env = cache._read_disk(h)
    from datetime import datetime, timedelta, timezone
    env["fetched_at"] = (datetime.now(timezone.utc) - timedelta(minutes=2)).isoformat()
    cache._write_disk(h, env)
    cache._mem.clear()  # 清 L1，强制走磁盘/刷新

    def boom():
        raise RuntimeError("backend down")

    r = cache.get("u@x.com", boom)
    assert r.stale is True
    assert r.payload["datasets"][0]["dataset_alias"] == "ds_a"


def test_get_raises_when_no_cache_and_fetch_fails(tmp_path: Path):
    """无任何缓存且拉取失败时原样抛错。"""
    cache = MetadataCache(base_dir=tmp_path)

    def boom():
        raise RuntimeError("backend down")

    with pytest.raises(RuntimeError):
        cache.get("fresh@x.com", boom)


def test_invalidate_specific_and_all(tmp_path: Path):
    """失效后下次强制重新拉取。"""
    cache = MetadataCache(base_dir=tmp_path)
    calls = {"n": 0}

    def fetch():
        calls["n"] += 1
        return _payload()

    cache.get("u@x.com", fetch)
    cache.invalidate("u@x.com")
    cache.get("u@x.com", fetch)
    assert calls["n"] == 2  # 失效后重新拉


def test_module_pool_same_instance(tmp_path: Path):
    """同 base_dir 返回同一实例（池化）。"""
    from opscli.query.services.metadata_cache import get_metadata_cache

    a = get_metadata_cache(base_dir=tmp_path)
    b = get_metadata_cache(base_dir=tmp_path)
    assert a is b
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_cache.py -k "stale or invalidate or pool or no_cache" -v`
Expected: FAIL — `get` 无 stale 分支 / `invalidate` 与 `get_metadata_cache` 未定义

- [ ] **Step 3: 写最小实现**

改 `get` 的拉取段，把 `payload = fetch_fn()` 那行替换为带兜底的版本：

```python
            try:
                payload = fetch_fn()
            except Exception:
                # 后端拉取失败：有过期缓存则回退并标 stale，否则原样抛出
                stale_env = env2 or env
                if stale_env is not None:
                    return self._result(stale_env, stale=True, from_cache=True)
                raise
```

在类末尾追加 `invalidate`：

```python
    def invalidate(self, user_email: str | None = None) -> None:
        """失效缓存。user_email=None 清全部用户；否则只清该用户。"""
        if user_email is None:
            with self._lock:
                self._mem.clear()
            if self._dir.exists():
                for f in self._dir.glob("*.json"):
                    try:
                        f.unlink()
                    except OSError:
                        pass
            return
        h = self._email_hash(user_email)
        with self._lock:
            self._mem.pop(h, None)
        try:
            self._cache_file(h).unlink()
        except OSError:
            pass
```

在模块末尾追加池：

```python
# 模块级缓存池：按 base_dir 隔离不同用户/客户端
_caches: dict[str | None, MetadataCache] = {}
_pool_lock = threading.Lock()


def get_metadata_cache(
    base_dir: Path | None = None, ttl_seconds: int = _DEFAULT_TTL_SECONDS
) -> MetadataCache:
    """获取池化的 MetadataCache（懒加载，按 base_dir 隔离）。"""
    key = str(base_dir) if base_dir else None
    if key not in _caches:
        with _pool_lock:
            if key not in _caches:
                _caches[key] = MetadataCache(base_dir=base_dir, ttl_seconds=ttl_seconds)
    return _caches[key]


def invalidate_metadata_cache(
    base_dir: Path | None = None, user_email: str | None = None
) -> None:
    """失效指定 base_dir 池的缓存（登录/登出时调用）。"""
    key = str(base_dir) if base_dir else None
    with _pool_lock:
        cache = _caches.get(key)
    if cache:
        cache.invalidate(user_email)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_cache.py -v`
Expected: PASS（13 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/query/services/metadata_cache.py tests/query/test_metadata_cache.py docs/change-log-pending.md
git commit -m "feat(query): 元数据缓存 stale 兜底/失效与模块级池"
```

---

## Task 7: QueryClient 全量字段参数

**Files:**
- Modify: `opscli/query/transport/client.py:126-165`
- Test: `tests/query/test_metadata_all.py`

**Interfaces:**
- Consumes: 现有 `QueryClient.fetch_query_metadata`。
- Produces: `fetch_query_metadata(*, dataset_alias=None, table_id=None, include_all_fields: bool = False) -> dict`——`include_all_fields=True` 时请求带 `include_all_fields=1`。Task 8 调用。

- [ ] **Step 1: 写失败测试**

创建 `tests/query/test_metadata_all.py`：

```python
"""QueryClient/QueryManager 全量元数据单测。"""
import httpx
import respx

from opscli.query.transport.client import QueryClient


@respx.mock
def test_fetch_query_metadata_include_all_fields():
    """include_all_fields=True 时请求带 include_all_fields=1。"""
    route = respx.get(url__regex=r".*/datasets/query-metadata.*").mock(
        return_value=httpx.Response(200, json={"code": 0, "data": {"datasets": [], "fields": [{"field_name": "x"}]}})
    )
    client = QueryClient()
    # 绕过真实认证：注入空鉴权
    client._get_auth = lambda system: ({}, {})  # type: ignore[method-assign]
    client.fetch_query_metadata(include_all_fields=True)
    assert route.called
    assert "include_all_fields=1" in str(route.calls.last.request.url)
```

> 若 `parse_remote_response` 对成功包裹体的 code/字段名断言不同，按 `opscli/shared/http.py` 实测结构调整 `json=` 内容（保持 `data.datasets`/`data.fields`）。

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_all.py::test_fetch_query_metadata_include_all_fields -v`
Expected: FAIL — `TypeError: fetch_query_metadata() got an unexpected keyword argument 'include_all_fields'`

- [ ] **Step 3: 写最小实现**

改 `opscli/query/transport/client.py` 的 `fetch_query_metadata`：签名加参数，params 组装加分支。

签名（126-131 行）改为：

```python
    def fetch_query_metadata(
        self,
        *,
        dataset_alias: str | None = None,
        table_id: int | None = None,
        include_all_fields: bool = False,
    ) -> dict:
```

在 params 组装段（`if table_id is not None:` 分支之后）追加：

```python
        # include_all_fields=1：让后端一次性返回全部授权数据集的全部字段
        # （规划器全量取数用；默认 false 保持裸调用"只列数据集"契约）
        if include_all_fields:
            params["include_all_fields"] = "1"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_all.py -v`
Expected: PASS（1 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/query/transport/client.py tests/query/test_metadata_all.py docs/change-log-pending.md
git commit -m "feat(query): QueryClient 支持 include_all_fields 全量字段"
```

---

## Task 8: QueryManager.metadata_all 经缓存返回全量

**Files:**
- Modify: `opscli/query/services/manager.py`（新增方法）
- Test: `tests/query/test_metadata_all.py`

**Interfaces:**
- Consumes: Task 6 `get_metadata_cache`、Task 7 `fetch_query_metadata(include_all_fields=True)`。
- Produces: `QueryManager.metadata_all(*, user_email: str, base_dir: Path | None = None) -> MetadataCacheResult`。后续规划器计划（Phase 4）与 CLI/MCP 入口调用。

- [ ] **Step 1: 写失败测试**

向 `tests/query/test_metadata_all.py` 追加：

```python
from pathlib import Path

from opscli.query.services.manager import QueryManager


@respx.mock
def test_metadata_all_uses_cache(tmp_path: Path):
    """metadata_all 首次拉取全量并缓存，二次命中不再请求。"""
    route = respx.get(url__regex=r".*/datasets/query-metadata.*").mock(
        return_value=httpx.Response(200, json={"code": 0, "data": {"datasets": [{"dataset_alias": "ds_a"}], "fields": [{"field_name": "x"}]}})
    )
    mgr = QueryManager()
    mgr.client._get_auth = lambda system: ({}, {})  # type: ignore[method-assign]

    r1 = mgr.metadata_all(user_email="u@x.com", base_dir=tmp_path)
    assert r1.from_cache is False
    assert r1.payload["fields"] == [{"field_name": "x"}]
    assert route.call_count == 1

    r2 = mgr.metadata_all(user_email="u@x.com", base_dir=tmp_path)
    assert r2.from_cache is True
    assert route.call_count == 1  # 命中缓存，未再请求
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/query/test_metadata_all.py::test_metadata_all_uses_cache -v`
Expected: FAIL — `AttributeError: 'QueryManager' object has no attribute 'metadata_all'`

- [ ] **Step 3: 写最小实现**

在 `opscli/query/services/manager.py` 顶部 import 区加：

```python
from pathlib import Path

from opscli.query.services.metadata_cache import (
    MetadataCacheResult,
    get_metadata_cache,
)
```

（`Path` 若已 import 则跳过。）在 `QueryManager.metadata` 方法后新增：

```python
    def metadata_all(
        self, *, user_email: str, base_dir: Path | None = None
    ) -> MetadataCacheResult:
        """获取当前用户的全量元数据（全部数据集全部字段），经用户级缓存。

        缓存未命中/过期时向后端 query-metadata?include_all_fields=1 拉取一次并落盘；
        1 小时内复用；后端失败时回退过期缓存并标 stale。

        Args:
            user_email: 当前用户邮箱（缓存隔离维度，由 CLI/MCP 调用方注入）。
            base_dir: 缓存根目录；CLI 默认 CONFIG_DIR，MCP 传隔离目录。

        Returns:
            MetadataCacheResult。
        """
        cache = get_metadata_cache(base_dir)
        return cache.get(
            user_email,
            lambda: self.client.fetch_query_metadata(include_all_fields=True),
        )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/query/test_metadata_all.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/query/services/manager.py tests/query/test_metadata_all.py docs/change-log-pending.md
git commit -m "feat(query): QueryManager.metadata_all 经用户级缓存返回全量元数据"
```

---

## Task 9: 登录/登出失效挂钩

**Files:**
- Modify: `opscli/auth/cli.py:70-115`（`login` 成功后、`logout` 内）
- Modify: `opscli/mcp/tools/auth.py`（在登录/登出工具已有的 `invalidate_credential_cache` 调用旁追加）
- Test: `tests/auth/test_login_cache_invalidation.py`

**Interfaces:**
- Consumes: Task 6 `invalidate_metadata_cache`。
- Produces: 无新接口；确立"登录成功/登出即失效元数据缓存"的行为（授权变化在登录发生）。

- [ ] **Step 1: 写失败测试**

创建 `tests/auth/test_login_cache_invalidation.py`：

```python
"""登录/登出失效元数据缓存挂钩单测。"""
from pathlib import Path

from opscli.query.services.metadata_cache import get_metadata_cache


def _payload():
    return {"datasets": [], "fields": [], "select_columns": []}


def test_logout_invalidates_metadata_cache(tmp_path: Path, monkeypatch):
    """logout 命令应清空默认 base_dir 的元数据缓存。"""
    # 预置一条缓存
    cache = get_metadata_cache(base_dir=tmp_path)
    cache.get("u@x.com", _payload)
    assert cache._read_disk(cache._email_hash("u@x.com")) is not None

    # 让 logout 使用的默认失效指向 tmp_path：patch CredentialStore.clear 为无副作用，
    # 并直接验证 invalidate_metadata_cache(base_dir=tmp_path) 生效
    from opscli.query.services.metadata_cache import invalidate_metadata_cache

    invalidate_metadata_cache(base_dir=tmp_path)
    assert cache._read_disk(cache._email_hash("u@x.com")) is None
```

> 说明：`login`/`logout` 命令本体走 Device Flow / Keychain，不宜在单测触发真实副作用；本测试锁定"失效函数正确清缓存"这一可验证内核。命令内是否正确调用该函数，由 Step 3 的接线 + 一次人工冒烟（`opscli auth logout` 后 `metadata/` 目录被清）确认。

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/auth/test_login_cache_invalidation.py -v`
Expected: 首次运行若 `invalidate_metadata_cache` 已存在（Task 6）则 PASS；本 Task 的重点是命令接线（Step 3）。若 Task 6 未完成会 ImportError。

- [ ] **Step 3: 接线到命令**

`opscli/auth/cli.py` 的 `logout` 命令改为（在 clear 后失效缓存，用 lazy import 避免 auth→query 模块级环依赖）：

```python
@app.command()
def logout():
    """清除本地所有凭证"""
    CredentialStore().clear()
    # 登出即失效元数据缓存（避免残留上一个账号的授权数据）
    from opscli.query.services.metadata_cache import invalidate_metadata_cache

    invalidate_metadata_cache()
    console.print("[green]√ 已退出，本地凭证已清除[/green]")
```

`login` 命令在成功打印后（`console.print(f"[green]√ 授权成功！...`）之前或之后追加：

```python
        # 登录成功后失效元数据缓存：授权范围可能随账号变化，强制下次重新拉取
        from opscli.query.services.metadata_cache import invalidate_metadata_cache

        invalidate_metadata_cache(user_email=result.get("email") or None)
```

`opscli/mcp/tools/auth.py`：在登录/登出工具**已有的** `invalidate_credential_cache(base_dir=_get_credential_dir())` 调用（187/282/323/359/420/548 行等对应登录成功与登出路径）旁，追加一行同 base_dir 的元数据失效。示例（登录成功路径）：

```python
            invalidate_credential_cache(base_dir=_get_credential_dir())
            # 同步失效该用户的元数据缓存
            from opscli.query.services.metadata_cache import invalidate_metadata_cache

            invalidate_metadata_cache(base_dir=_get_credential_dir())
```

> 仅在登录成功与登出两类语义处追加；纯 token 刷新路径不必失效元数据。

- [ ] **Step 4: 运行测试与冒烟**

Run: `pytest tests/auth/test_login_cache_invalidation.py -v`
Expected: PASS（1 passed）

人工冒烟（可选，需真实登录态）：`opscli auth logout` 后确认 `~/.config/opscli/metadata/` 下缓存文件被清空。

- [ ] **Step 5: 记录变更并提交**

```bash
git add opscli/auth/cli.py opscli/mcp/tools/auth.py tests/auth/test_login_cache_invalidation.py docs/change-log-pending.md
git commit -m "feat(auth): 登录/登出失效用户级元数据缓存"
```

---

## Task 10: 全量回归与依赖方向校验

**Files:**
- Test: 全量测试套件

**Interfaces:** 无。

- [ ] **Step 1: 跑全量测试**

Run: `pytest tests/ -v`
Expected: 全绿（新增用例 + 原有用例不回归）

- [ ] **Step 2: 校验依赖方向（铁律2）**

Run: `grep -rn "import opscli.mcp\|from opscli.mcp" opscli/query/`
Expected: 无输出（query 未反向依赖 mcp）

- [ ] **Step 3: 校验两种 SDK 导入仍可用（铁律3）**

Run: `python -c "from opscli import AuthClient; from opscli.auth import AuthClient; print('ok')"`
Expected: 输出 `ok`

- [ ] **Step 4: 提交收尾**

```bash
git add docs/change-log-pending.md
git commit -m "test: 用户级元数据缓存基础设施全量回归通过"
```

---

## Self-Review（作者自查）

**Spec coverage：**
- Phase 1 file_lock → Task 1 ✓
- Phase 2 缓存（两层/信封/TTL/email 哈希命名/stale/失效/池） → Task 2-6 ✓
- Phase 3 QueryClient/Manager 全量 → Task 7-8 ✓
- Phase 5 登录失效 → Task 9 ✓
- 决策：TTL 1h（Task 2 `_DEFAULT_TTL_SECONDS=3600`）、stale 兜底（Task 6）、email 哈希命名（Task 2 `_email_hash` + Task 3 `_cache_file`） ✓
- 未覆盖（有意，范围外）：Phase 0 后端 PHP、Phase 4 规划器迁入——已在"范围边界"声明，各自独立计划。

**类型一致性：** `MetadataCacheResult(payload, stale, fetched_at, from_cache)`、`MetadataCache.get(user_email, fetch_fn)`、`invalidate(user_email=None)`、`get_metadata_cache(base_dir, ttl_seconds)`、`invalidate_metadata_cache(base_dir, user_email)`、`fetch_query_metadata(..., include_all_fields=False)`、`metadata_all(user_email, base_dir)` 全程命名一致。

**占位符扫描：** 无 TODO/TBD；每个代码步骤含完整可运行代码与真实测试。

**并发任务的条件分支（Task 5 Step 3）：** 属"测试暴露缺陷才执行"的修复步骤，非占位符——给出了完整修复代码。
</content>
