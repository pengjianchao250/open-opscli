# CHANGELOG

## 0.5.4 - 2026-05-27

- 新增：把“总结/盘点已有运营知识”“判断哪些值得做成 Skills”“规划一组 Skills 避免重复建设”写入主触发描述，明确这类请求必须先触发 `ops-creator-skill`。
- 新增：`资产去重门`，要求知识盘点或批量 Skill 规划时先列现有 Skill 资产、能力重叠、建议复用项和真实缺口，再决定新建或改造。
- 更新：主流程新增 `知识盘点/Skill 规划` 分类和 `资产盘点门`，避免直接跳到执行层或直接新建重复 Skill。
- 更新：`agents/openai.yaml` 默认提示词，强调先做资产盘点和去重，再进入访谈、数据预检和正式创建。
- 新增：回归测试加入“经验盘点与 Skill 规划”静态用例和模拟对话用例，防止再次漏触发。

## 0.5.3 - 2026-05-27

- 恢复：顶层 `AGENTS.md`、`CHANGELOG.md`、`VERSIONING.md` 作为本 Skill 的维护文档继续保留。
- 回退：删除 `references/changelog.md`，版本记录重新回到顶层 `CHANGELOG.md` / `VERSIONING.md`。
- 更新：`SKILL.md` 的当前版本号改为 `0.5.3`，并改为引用顶层版本文档。
- 更新：`.gitignore` 不再忽略顶层维护文档。
- 更新：`scripts/package_submission.py` 不再排除 `AGENTS.md`、`CHANGELOG.md`、`VERSIONING.md`。

## 0.5.2 - 2026-05-27

- 新增：`scripts/regression_tests.py` 升级为“静态策略扫描 + 模拟对话输出测试”双层回归。
- 新增：8 个用户场景的模拟执行器测试，检查首轮响应是否会正确停在价值判断、访谈门、数据预检、旧 Skill 影响点扫描或轻量替代物。
- 新增：回归报告输出静态用例数、模拟对话用例数、通过/失败和缺失项。
- 更新：主文件记录当前版本，并把版本历史放入 `references/changelog.md`，避免恢复顶层维护文档。

## 0.5.1 - 2026-05-27

- 新增：8 个场景的脚本化静态回归测试。
- 新增：防止把测试样例写成通用硬规则的扫描项。
- 更新：旧 Skill 改造从固定确认平台/名称，改为先做影响点扫描，只确认本次实际受影响事项。
- 更新：`existing-skill-retrofit.md` 和 `skill-creator-handoff.md` 使用影响点驱动的改造流程。

## 0.5.0 - 2026-05-27

- 重写：`SKILL.md` 压缩为轻量入口，只保留触发边界、硬门槛、主流程、交付闭环和按需读取。
- 新增：不适合创建 Skill 的边界，避免把一次性任务、单条文案、泛泛想法强行包装成 Skill。
- 新增：访谈阶段硬门槛，Q1-Q6 和阶段 1 未确认前不得创建或更新正式文件。
- 新增：AI 代答隔离规则，所有推测必须标 `[AI 假设，待用户确认]`。
- 新增：内部数据预检硬门槛，字段、过滤、dry-run 和 smoke query 不完整时不得写成正式取数流程。
- 新增：必须交给当前环境 `skill-creator` 或同类能力正式落地，不把流程 brief 当成完整交付。
- 新增：安装、校验、测试清单、可安装包或无需打包理由作为完成定义。
- 清理：删除运行时维护文件和 Python 缓存，打包脚本排除维护态文件。

## 0.4.2 - 2026-05-19

- 新增：生成 Skill 的默认执行策略，要求已固化流程日常直接执行，只在异常、测试、优化、发布或用户要求解释时展开额外分析。
- 更新：统一框架、handoff、流程简报模板和 `brief_to_skill.py`，区分“生成/更新前取数预检”和“日常执行已验证取数规则”。
- 优化：按需加载策略从“默认 1-2 个 reference”调整为“默认 0-2 个 reference”，进一步降低常规执行 token 负担。
- 验证：重新编译生成脚本，并用流程简报模板生成草案做冒烟检查。

## 0.4.1 - 2026-05-19

- 新增：阶段 0 访谈的交互式提问与降级规则，支持“结构化提问 UI 优先、聊天选项降级、表单/HTML 兜底”。
- 更新：主流程明确结构化提问只是增强体验，不作为跨工具复用的硬依赖；没有按钮式 UI 时继续用聊天选项推进。
- 验证：补充静态规则检查，并重新编译脚本。

## 0.4.0 - 2026-05-18

- 新增：运营 Skill 统一框架标准，约束新建和改造 Skill 默认采用轻量 `SKILL.md`、按需 `references/`、稳定 `scripts/` 的结构。
- 新增：已有运营 Skill 归一化流程，要求先建立旧版基线、保护已有行为、共同修正数据/字段/依赖，再做同源对比。
- 更新：`brief_to_skill.py` 生成器默认输出轻量主文件，并把数据方案、完整规则、测试、跨工具、发布治理拆分到 references。
- 更新：流程简报模板增加“统一框架与加载策略”，用于记录默认加载资料和 token 优化方式。
- 更新：handoff 规范增加统一框架、旧 Skill 改造和上下文预算检查。

## 0.3.2 - 2026-05-18

- 修复：候选资格判断尊重 `real_business_input: false`，模拟样本不会因为存在数据来源摘要而被计入真实业务输入。
- 增强：对 `模拟`、`synthetic`、`mock`、`sample` 标记的输入摘要做保守识别，降低误提交风险。
- 验证：已重新编译 `scripts/qualify_candidate.py`。

## 0.3.1 - 2026-05-18

- 修复：候选提交打包脚本保留中文提交人目录名，避免中文姓名被替换成 `-`。
- 验证：已重新编译 `scripts/package_submission.py`。

## 0.3.0 - 2026-05-18

- 新增：内测 Skill 提交治理，定义 `personal_draft`、`candidate`、`team_beta`、`standard` 生命周期。
- 新增：执行日志与学习日记规范，支持 `[RUN]`、`[ERR]`、`[LRN]`、`[FEAT]`、`[REF]` 分层记录。
- 新增：`scripts/qualify_candidate.py`，根据运行日志判断 Skill 是否达到候选提交门槛。
- 新增：`scripts/package_submission.py`，生成脱敏提交包、运行摘要、安全检查和提交元数据。
- 更新：流程简报模板和生成脚本，后续生成的运营 Skill 默认带执行日志、候选提交规范和提交脚本。
- 验证：已用模拟运行日志测试候选拦截、达标判断和脱敏打包流程。

## 0.2.2 - 2026-05-18

- 版本管理：将阅读并吸收《重新定义Skill开发》文章之前的版本保存为初始版。
- 新增 tag：`v0.1.0`，位于 `pre-article-initial` 分支，代表文章前的初始版本。
- 保持当前可用版本在 `main` 分支，不切换用户日常调用的 Skill 到旧版。

## 0.2.1 - 2026-05-18

- 新增：本地 git 版本管理规则，避免后续修改静默覆盖已确认版本。
- 新增：`VERSIONING.md`，记录查看、修改、提交、打 tag 和恢复旧版的轻量流程。
- 新增：`AGENTS.md`，提醒后续维护时先检查版本状态并更新变更记录。
- 新增：`.gitignore`，排除本地缓存、临时输出和打包产物。
- 状态：当前版本作为本地维护稳定版。

## 0.2.0 - 2026-05-18

- 新增：阶段 0 业务 6 维访谈与阶段 1 技术补充流程。
- 新增：ops 内部数据取数意图识别、数据集推荐、字段聚合、取数预检和 CSV/XLSX 降级方案。
- 新增：跨 AI 工具兼容方案、Python 脚本固化、HTML 展示、测试与基准对比。
- 新增：发布治理、执行日志和自我迭代评测兜底。
- 新增：`brief_to_skill.py`，可从流程简报生成中文 Skill 草案目录。
- 状态：当前版本作为本地稳定版冻结，后续修改应先查看差异并另行提交。
